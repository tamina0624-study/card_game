# 進捗

このファイルはタスクごとの進捗状況を記録する。実装担当エージェントは着手時に「未着手」→「作業中」、完了時に「完了」へ更新し、「メモ」欄に発生した問題・試したこと・残課題を追記すること。

設計の詳細は `docs/設計.md` を参照。

## タスク一覧

- [x] 1. プロジェクト初期化(Next.js + TypeScript + SQLiteセットアップ)
  - 状態: 完了
  - メモ:
    - `create-next-app@latest`(TypeScript / App Router / ESLint有効 / `src/`ディレクトリ / import alias `@/*`)でプロジェクトを生成。
      既存のドキュメント類(README.md, docs/, 進捗.md, 開発指示書.md, システムプロンプト.md等)と衝突するため、
      一時ディレクトリで scaffold した後に生成物(`.gitignore`, `eslint.config.mjs`, `next-env.d.ts`, `next.config.ts`,
      `package.json`, `package-lock.json`, `public/`, `src/`, `tsconfig.json`)のみをリポジトリ直下へコピーする手順を取った
      (`create-next-app`はディレクトリに他ファイルが存在すると実行を拒否するため)。
    - `package.json` の `name` を `card_game_init` から `card_game` に修正。
    - 依存関係を追加: `dependencies` に `better-sqlite3`, `@anthropic-ai/sdk`, `zod`, `uuid`。
      `devDependencies` に `tsx`(マイグレーション/シードスクリプトのts実行用), `@types/better-sqlite3`
      (`uuid`は同梱の型定義があるため`@types/uuid`は不要と判断し追加せず)。
    - `.env.example` をルートに作成し `ANTHROPIC_API_KEY=` / `ANTHROPIC_MODEL=claude-opus-4-8` / `DB_PATH=./data/game.db` を記載。
    - `.gitignore` を更新: `data/*.db`・`data/*.db-journal`、`public/uploads/characters/*`(`.gitkeep`は除外解除)、
      `.env.local` を追加。デフォルトの `.env*` ignoreだと `.env.example` も無視されてしまうため
      `!.env.example` を追加して明示的に追跡対象に戻した。
    - `public/uploads/characters/.gitkeep` と `data/.gitkeep` を作成しディレクトリ自体は追跡できるようにした
      (中身のDBファイル・アップロード画像は無視される)。
    - `package.json` に `db:migrate`(`tsx src/lib/db/migrate.ts`)・`db:seed`(`tsx src/lib/db/seed.ts`)のnpmスクリプト枠を追加。
      実処理は後続タスク(2, 3)で実装するため、現時点では `src/lib/db/migrate.ts` / `seed.ts` はTODOコメント付きの
      プレースホルダ(console.logのみ)として作成。両ファイルとも `export {}` を末尾に付与し、TypeScriptが
      2つの `.ts` ファイルを「スクリプト」として扱い `main` 関数名が衝突するビルドエラー
      (`Duplicate function implementation`)を回避した。
    - 動作確認: `npm run build`(型チェック含め成功)、`npm run lint`(エラーなし)、`npm run db:migrate` / `npm run db:seed`
      (プレースホルダメッセージが出力されること)、`npm run dev -- -p 3411` を起動し `curl http://localhost:3411/` で
      HTTP 200・デフォルトページのHTMLが返ることを確認済み。また `better-sqlite3` / `uuid` / `@anthropic-ai/sdk` / `zod`
      を node -e で実際に `require` してAPIが動作することを確認した(better-sqlite3のネイティブビルドも問題なし)。
    - 残課題: `src/lib/db/schema.sql`, `client.ts`, 本実装の `migrate.ts` / `seed.ts` はタスク2・3で実装する
      (今回作成した2ファイルはプレースホルダなので、タスク2着手時に上書きしてよい)。

- [x] 2. SQLiteスキーマ定義 + マイグレーションスクリプト実装
  - 状態: 完了
  - メモ:
    - `src/lib/db/schema.sql` に `docs/設計.md` 2章のDDLをそのまま作成(`characters`, `character_parameters`,
      `special_moves`, `decks`, `deck_cards`, `battles`, `battle_logs`, `battle_events` の全8テーブル + 各種
      `CREATE INDEX IF NOT EXISTS`)。すべて `CREATE TABLE IF NOT EXISTS` で冪等に実行できる形。
      (タスク説明文には「全7テーブル」とあったが、設計書2章に列挙されたテーブル名は8個であり、実際に作成されるのも
      8テーブルなので、設計書の記載を正として8テーブルすべてを実装した。)
    - `src/lib/db/client.ts` に `getDb()` を実装。`better-sqlite3` の接続をモジュールスコープの変数
      (`dbInstance`)にキャッシュしてプロセス内シングルトンとして返す。`DB_PATH` 環境変数を
      `process.cwd()` 基準で解決(未設定時は `./data/game.db`)し、接続先ディレクトリが無ければ
      `fs.mkdirSync(dir, { recursive: true })` で作成、接続確立時に必ず `db.pragma("foreign_keys = ON")`
      を実行するようにした。
    - `src/lib/db/migrate.ts`(タスク1で作成したプレースホルダを本実装に置き換え)は `__dirname` 基準で
      `schema.sql` を読み込み `getDb().exec(schemaSql)` で実行するのみのシンプルな実装。
      `npm run db:migrate`(`tsx src/lib/db/migrate.ts`)から実行可能(npmスクリプト自体はタスク1で追加済み)。
    - 動作確認:
      - `rm -f data/game.db && npm run db:migrate` を実行し `data/game.db` が新規生成されることを確認。
      - `sqlite3 data/game.db ".tables"` / `".schema"` で8テーブル全て(CHECK制約・INDEX含む)が
        設計書どおりに作成されていることを確認。
      - `npm run db:migrate` を2回連続実行してもエラーにならないこと(`IF NOT EXISTS` による冪等性)を確認。
      - `DB_PATH` に存在しない深いパス(例: `.../nested/mytest.db`)を指定して実行し、親ディレクトリが
        自動作成され、そこにも8テーブルが作成されることを確認(scratchpad配下で検証、検証後削除済み)。
      - `node` から直接 `better-sqlite3` で接続し `PRAGMA foreign_keys` が有効(`1`)であること、
        `characters` 行削除時に `character_parameters` の関連行が `ON DELETE CASCADE` で連動削除される
        ことを確認。
      - `npm run build`(Next.js本体のビルド、TypeScript型チェック含む)・`npm run lint` がともに
        エラーなく成功することを確認。
    - 残課題: なし。次タスク(3. シードスクリプト実装)で `src/lib/db/seed.ts` を本実装する。

- [x] 3. シードスクリプト実装(開発・確認用サンプルデータ投入)
  - 状態: 完了
  - メモ:
    - `src/lib/db/seed.ts`(タスク1のプレースホルダを本実装に置き換え)を実装。`getDb()` で
      `data/game.db` に接続し、`db.transaction()` 内で以下を実行する単一のスクリプトとした。
    - サンプルキャラクターを16体、TypeScriptの配列リテラル(`CHARACTERS`)としてハードコード。
      各キャラクターは自由なパラメータ名(`攻撃`/`防御`/`素早さ`/`体力`/`魔力` など3〜5個)を持ち、
      パラメータ合計は全キャラクター100以下(85〜100の範囲)になるよう手計算で調整した。各キャラクター
      必殺技(`name`/`description`/`flavorText`)を1〜2個持つ(`special_moves.flavor_text` 列に対応)。
      `characters.total_points` はパラメータ合計をスクリプト側で計算してINSERT時に格納(まだ
      タスク4のバリデーション/リポジトリ層が無いため、CRUD API実装を待たずrawなINSERT文で投入)。
    - 16体のうち前半8体で「サンプルデッキA」(前衛4/控え4)、後半8体で「サンプルデッキB」
      (前衛4/控え4)を作成し `decks` / `deck_cards`(`role='front'|'bench'`, `slot_order`)に投入。
      これによりタスク15以降で対戦実行(デッキA vs デッキB)をすぐに試せる状態にした。
    - 重複投入防止策: スクリプト冒頭で `battle_events` → `battle_logs` → `battles` → `deck_cards` →
      `decks` → `special_moves` → `character_parameters` → `characters` の順(子→親)で対象8テーブルを
      全件DELETEしてから再投入する設計にした(何度 `npm run db:seed` を実行しても件数が変わらない)。
      あわせて `sqlite_sequence`(AUTOINCREMENTカウンタ管理テーブル)が存在する場合のみ対象テーブル分を
      DELETEし、再実行時にキャラクターID/デッキIDが常に1始まりになるようにした(初回マイグレーション
      直後など `sqlite_sequence` テーブル自体が未作成のケースがあるため `sqlite_master` で存在確認してから
      DELETEするガードを入れた)。
    - キャラクター名→IDの解決には投入時に構築した `Map<string, number>` を使用し、デッキの
      front/bench配列(キャラクター名で指定)からdeck_cardsのINSERTに必要な `character_id` を解決。
      データ不整合(パラメータ合計100超過、前衛/控えが4件でない、存在しないキャラクター名を参照)は
      スクリプト内でthrowして即座に気づけるようにした(現状のシードデータでは発生しない想定の防御的チェック)。
    - `package.json` の `db:seed`(`tsx src/lib/db/seed.ts`)はタスク1で追加済みのため変更不要だった。
    - 動作確認:
      - `rm -f data/game.db && npm run db:migrate && npm run db:seed` を実行し、正常終了メッセージ
        (`[db:seed] サンプルキャラクター16体・サンプルデッキ2件を投入しました。`)を確認。
      - `better-sqlite3` を直接requireしたNodeスクリプトで `characters`=16件, `character_parameters`=67件,
        `special_moves`=24件, `decks`=2件, `deck_cards`=16件であることを確認。
      - `total_points > 100` のキャラクターが0件であること、各デッキが `front`4件/`bench`4件ちょうど
        であること、各キャラクターの必殺技数が1〜2件の範囲に収まっていることをSQLクエリで確認。
      - `npm run db:seed` をもう一度実行し(既存データがある状態での再実行)、件数が変わらず
        (`characters`=16, `decks`=2, `deck_cards`=16)、IDも1始まりにリセットされる(冪等)ことを確認。
      - `npm run build`(Next.js本体のビルド、TypeScript型チェック含む)・`npm run lint` がともに
        エラーなく成功することを確認。
    - 残課題: なし。次タスク(4. キャラクターDTO/バリデーション実装)以降でCRUD API・リポジトリ層を
      実装した際、このシードデータで作成したキャラクター/デッキがAPI経由でも正しく参照できることを
      あわせて確認するとよい。

- [x] 4. キャラクターDTO/バリデーション実装(100ポイント制限チェック)
  - 状態: 完了
  - メモ:
    - `src/lib/types.ts` を新規作成し、共有TypeScript型を定義。`CharacterParameterInput`
      (`{name, value}`)、`SpecialMoveInput`(`{name, description?, flavorText?}`)、
      `CharacterInput`(`{name, description?, imageUrl?, parameters, specialMoves?}`、
      docs/設計.md 3章のリクエストボディ形状に対応)に加え、取得系APIの戻り値として
      `CharacterParameter`/`SpecialMove`(id・sortOrder付き)、`Character`
      (`totalPoints`含む全情報)も定義した(タスク5のCRUD API実装で使う想定)。
    - `src/lib/characters/validation.ts` を新規作成。zod (v4系, package.jsonの`zod@^4.4.3`)で
      `parameterSchema`(name: 1文字以上・trim、value: 0〜100の整数)、`specialMoveSchema`
      (name: 1文字以上・trim、description/flavorTextは任意文字列)、`characterInputSchema`
      (name: 1文字以上、description/imageUrlは任意、parameters: parameterSchemaの配列で
      最低1件、specialMoves: specialMoveSchemaの配列で任意)を実装。
    - `characterInputSchema` に `.superRefine()` を追加し、`parameters[].value` の合計が
      `MAX_TOTAL_POINTS`(=100)を超える場合に `ctx.addIssue({ code: "custom",
      path: ["parameters"], message: "パラメーターの合計は100ポイント以下にしてください" })`
      でカスタムissueを発行するようにした(zod v4では `code` にリテラル文字列 `"custom"`
      を渡す形になっており、`z.ZodIssueCode.custom` は非推奨エイリアスだったため素の
      文字列を採用)。
    - `validateCharacterInput(input: unknown)` をヘルパー関数としてエクスポート。内部で
      `characterInputSchema.safeParse(input)` を呼び、成功時は `{ success: true, data }`
      (dataは検証・trim後の値)、失敗時は `{ success: false, errors: z.ZodError<CharacterInput> }`
      を返す(`errors.issues` でパス付きの日本語メッセージ一覧を取得できる)。
    - タスク説明の「このチェックはキャラクター登録・更新時のみ行い、AI審判側には一切依存
      しない設計であること」をファイル冒頭のコメントに明記した。実際にも
      `src/lib/characters/validation.ts` は `zod` と `@/lib/types` のみに依存し、
      `lib/claude` や `lib/battles` 配下は一切importしていない(依存グラフ上も
      AI審判側から完全に独立していることを確認済み)。
    - 動作確認:
      - `npm run build`(Next.js本体のビルド、TypeScript型チェック含む)、`npm run lint`
        がともにエラーなく成功することを確認。
      - scratchpad配下に一時テストスクリプトを作成し `npx tsx` で実行し、以下の9パターンを
        すべて期待通りに検証できることを確認(検証後にスクリプトは削除済み):
        (1) パラメーター合計ちょうど100→OK、(2) 合計90(100未満)→OK、
        (3) 合計101(超過)→NG(`path: ["parameters"]`、日本語メッセージ)、
        (4) キャラクター名が空文字→NG、(5) parametersが0件→NG、
        (6) パラメーター単体の値が101(範囲外)→NG(値の範囲外エラーと合計超過エラーの
        両方が出ることを確認)、(7) パラメーター値が小数→NG(整数チェック)、
        (8) パラメーター名が空文字→NG、(9) specialMoves省略→OK(任意項目であることを確認)。
    - 残課題: なし。次タスク(5. キャラクターCRUD API実装)で `validateCharacterInput` と
      `src/lib/types.ts` の型をそのままAPI Route Handler・リポジトリ層から利用する想定。

- [x] 5. キャラクターCRUD API実装
  - 状態: 完了
  - メモ:
    - `src/lib/characters/repository.ts` を新規作成。`getDb()`(`lib/db/client.ts`)を用いて
      `createCharacter(input)` / `listCharacters()` / `getCharacterById(id)` /
      `updateCharacter(id, input)` / `deleteCharacter(id)` を実装。
    - 作成・更新は `db.transaction()` 内で「`characters` へのINSERT/UPDATE」→
      「`character_parameters`・`special_moves` を `DELETE FROM ... WHERE character_id = ?`
      で全削除→入力配列を `sort_order`(配列index)付きで再INSERT」の順に実行する共通処理
      (`replaceParametersAndMoves`)を作成・更新の両方から呼び出す設計にした。
      `characters.total_points` は `parameters[].value` の合計をアプリ側(`computeTotalPoints`)
      で計算してINSERT/UPDATE時に保存する(バリデーション済みの `CharacterInput` を受け取る前提、
      タスク4の `validateCharacterInput` が100ポイント制約を検証済み)。
    - `deleteCharacter(id)` はトランザクション内で (1) 対象が存在しなければ `false` を返す
      (呼び出し元は404)、(2) `SELECT 1 FROM deck_cards WHERE character_id = ?` で使用中か判定し、
      該当すれば独自例外 `CharacterInUseError`(`code: "CHARACTER_IN_USE"`、`characterId`
      プロパティ付き)を投げて削除を拒否する(呼び出し元は409)、(3) 未使用なら
      `DELETE FROM characters` を実行し `true` を返す、という3分岐にした。
      `CharacterInUseError` をrepositoryからexportし、route.ts側で `instanceof` 判定して
      409に変換できるようにした。
    - `listCharacters()`/`getCharacterById()` は `characters` 1行 + 関連する
      `character_parameters`・`special_moves`(`sort_order ASC, id ASC` 順)を都度取得して
      `Character` 型(`lib/types.ts`、タスク4で定義済み)に組み立てる共通ヘルパー
      (`assembleCharacter`/`findCharacterById`)を用いる設計(N+1クエリになるが、MVPの
      想定データ量(数十件程度)では問題にならないと判断し、シンプルさを優先した)。
    - `src/lib/types.ts` に `CharacterSummary` 型(`id/name/description/imageUrl/totalPoints/
      parameterCount/specialMoveCount/createdAt/updatedAt`)を追加し、一覧APIのレスポンス形状
      として使用(docs/設計.md 3章「パラメータ合計・必殺技数を含む簡易情報」に対応)。
    - `src/app/api/characters/route.ts` を新規作成。ファイル先頭に
      `export const runtime = "nodejs";` を明記(SQLite同期アクセスのためEdge Runtime不可)。
      `GET` は `listCharacters()` の結果を `CharacterSummary` へ変換して200で返す。
      `POST` は `request.json()` のパース失敗時400、`validateCharacterInput` 失敗時400
      (`{ error, details: issues }`)、成功時 `createCharacter` を呼び出し201で作成物
      (詳細情報)を返す。
    - `src/app/api/characters/[id]/route.ts` を新規作成(同じく `runtime = "nodejs"` を先頭に明記)。
      Next.js 16(App Router)の動的ルートパラメータは非同期(`params: Promise<{ id: string }>`)
      のため `await context.params` で取得する。`id` パスパラメータは正規表現
      (`/^\d+$/`)で正の整数のみ許容し、不正なら400を返す `parseCharacterId` ヘルパーを実装。
      - `GET`: `getCharacterById` が `null` なら404、あれば200で詳細を返す。
      - `PUT`: JSON不正・`validateCharacterInput` 失敗で400、`updateCharacter` が
        `null`(対象なし)なら404、成功時200で更新後の詳細を返す。
      - `DELETE`: `deleteCharacter` を `try/catch` で呼び出し、`false` 返却時404、
        `CharacterInUseError` 捕捉時409、成功時204(本文なし)を返す。
    - `better-sqlite3` の型は `lib/db/client.ts` と同じ流儀(`import Database from
      "better-sqlite3"` のうえで型注釈は `Database.Database` を使用、`export =` 形式の
      型定義のため名前付き `import type { Database }` は使えないことを確認した上で統一)。
    - 動作確認:
      - `npm run build`(Next.js本体のビルド、TypeScript型チェック含む)・`npm run lint`
        がともにエラーなく成功することを確認(`next.config.ts` 経由の
        「Encountered unexpected file in NFT list」warningはTurbopackのトレース警告であり
        ビルド失敗ではない。DBパス解決に `path.resolve(process.cwd(), ...)` を使っている
        ことに起因する既知の情報warningで、他タスクのファイルにも影響する範囲外の事象と判断)。
      - `npm run db:migrate && npm run db:seed` 実行後、`npm run dev -- -p 3512` で
        実サーバーを起動し、以下をすべて `curl` で確認(検証後 `npm run db:seed` を再実行し
        シードデータを元の16体/2デッキの状態に戻して終了):
        - `GET /api/characters` → 200、16件、`CharacterSummary` 形状
          (`id/name/description/imageUrl/totalPoints/parameterCount/specialMoveCount/
          createdAt/updatedAt`)であることを確認。
        - `GET /api/characters/1` → 200、`parameters`/`specialMoves` 全件を含む詳細
          であることを確認。`GET /api/characters/9999` → 404。`GET /api/characters/abc`
          → 400(id不正)。
        - `POST /api/characters`(合計80ポイントの正常な入力)→ 201、作成物(id=17)が
          返ることを確認。
        - `POST /api/characters`(合計110ポイント・超過)→ 400
          (`path: ["parameters"]` の日本語エラー)。`parameters: []`(0件)→ 400。
          不正なJSON文字列ボディ → 400。
        - `PUT /api/characters/17`(正常な更新)→ 200、`totalPoints`
          再計算・`updatedAt` 更新を確認。`PUT /api/characters/9999` → 404。
          `PUT /api/characters/17`(name空文字)→ 400。
        - `DELETE /api/characters/1`(サンプルデッキAの前衛として使用中)→ 409
          (エラーメッセージに `characterId` 相当の情報を含む)。
          `DELETE /api/characters/17`(どのデッキにも未使用)→ 204。
          その後同じidを再度 `DELETE` → 404。存在しないid(99999)の `DELETE` → 404。
      - 上記いずれも設計書3章のステータスコード方針(作成201・詳細/一覧200・更新200・
        削除204・検証失敗400・未存在404・使用中409)どおりであることを確認した。
    - 残課題: なし。次タスク(6. 画像アップロードAPI実装)で `imageUrl` を実際に
      画像アップロードAPI経由で設定するフローを実装する想定(今回のCRUD APIは
      `imageUrl` を任意の文字列としてそのまま保存・返却するのみ)。

- [x] 6. 画像アップロードAPI実装
  - 状態: 完了
  - メモ:
    - `src/lib/uploads/storage.ts` を新規作成し `saveCharacterImage(file: File): Promise<{ url: string }>`
      を実装。許容MIMEタイプ(`image/png`, `image/jpeg`, `image/webp`, `image/gif`、
      `ALLOWED_IMAGE_MIME_TYPES` として定数化)とファイルサイズ上限2MB
      (`MAX_IMAGE_SIZE_BYTES = 2 * 1024 * 1024`)を検証し、違反時は識別可能なエラー
      `ImageValidationError`(`code: "INVALID_FILE_TYPE" | "FILE_TOO_LARGE"`、
      `lib/characters/repository.ts` の `CharacterInUseError` と同様の設計方針)を投げる。
    - ファイル名は `uuid`(v4)で生成。「元の拡張子を保持」の要件について、単純に
      アップロードファイル名の拡張子をそのまま使うと二重拡張子等でMIMEタイプと矛盾する
      拡張子(例: `photo.png.php` を `image/png` として送る等)を書き込んでしまう懸念が
      あったため、`resolveExtension()` で「元の拡張子が検証済みMIMEタイプに対応する
      許容拡張子一覧(`.png`/`.jpg`・`.jpeg`/`.webp`/`.gif`)に含まれる場合のみ元の拡張子を
      採用し、含まれない・拡張子が無い場合はMIMEタイプから決まる既定拡張子にフォールバック
      する」設計とした。
    - `public/uploads/characters/` ディレクトリが無ければ `fs.promises.mkdir(dir, { recursive: true })`
      で作成した上で `fs.promises.writeFile()` でファイルを書き込み、
      `{ url: "/uploads/characters/<生成ファイル名>" }` を返す。
    - `src/app/api/upload/image/route.ts` を新規作成(先頭に `export const runtime = "nodejs";` を
      明記、ローカルファイルシステムへの書き込みのため)。`POST` は `request.formData()` を
      `try/catch` でパースし(失敗時400)、`file` フィールドを取得して `File` インスタンスで
      なければ400、`saveCharacterImage` を呼び出し成功時200で `{ url }` を返す。
      `ImageValidationError` を捕捉した場合は400でエラーメッセージ(`error.message`)を返す
      設計とした(既存の `api/characters` 系Route Handlerと同じエラーレスポンス形状
      `{ error: string }` に統一)。
    - `public/uploads/characters/.gitkeep` はタスク1で既に作成済みであることを確認
      (今回新規作成は不要)。`.gitignore` の
      `public/uploads/characters/*` / `!public/uploads/characters/.gitkeep`
      もタスク1で設定済みであることを確認し、変更不要と判断した。
    - 動作確認:
      - `npm run build`(Next.js本体のビルド、TypeScript型チェック含む)・`npm run lint`
        がともにエラーなく成功することを確認(既知の `next.config.ts` 経由の
        「Encountered unexpected file in NFT list」warningのみで、ビルド失敗ではない。
        タスク5のメモに記載済みの既知事象で本タスクとは無関係)。
      - `npm run dev -- -p 3611` で実サーバーを起動し、以下をすべて `curl -F` で確認
        (検証後アップロードした一時ファイルは削除しディレクトリを `.gitkeep` のみの状態に
        戻して終了):
        - 1x1の実PNG画像(`image/png`)をアップロード → 200、
          `{"url":"/uploads/characters/<uuid>.png"}` が返り、`public/uploads/characters/`
          に実ファイルが書き込まれ、`GET /uploads/characters/<uuid>.png`(Next.jsの静的配信)
          でも200が返ることを確認。
        - 2MB超(2*1024*1024+100バイト)のダミーファイルを `image/jpeg` として
          アップロード → 400(「ファイルサイズが上限(2MB)を超えています。」)。
        - `text/plain` のファイルをアップロード → 400
          (「対応していないファイル形式です。png/jpeg/webp/gif形式の画像のみ
          アップロードできます。」)。
        - `file` フィールドを付けずにリクエスト → 400
          (「画像ファイル(file)を指定してください。」)。
        - 拡張子が `.php`(実体はPNG、`image/png` として送信)のファイル、および
          拡張子無しのファイルをそれぞれ `image/png` としてアップロード → いずれも200、
          `resolveExtension()` のフォールバックによりMIMEタイプ由来の `.png` で保存され、
          不審な拡張子(`.php`)や拡張子無しでファイルが書き込まれないことを確認。
    - 残課題: なし。次タスク(7. デッキDTO/バリデーション + デッキCRUD API実装)以降で
      `POST /api/characters` の `imageUrl` にこのAPIが返した `url` をフロントエンドから
      設定するフロー(タスク13のキャラクター作成画面)を実装する想定
      (今回のタスクは画像保存API単体の実装のみで、キャラクターCRUD APIとの結線はフロント側で行う)。

- [x] 7. デッキDTO/バリデーション + デッキCRUD API実装(8枚・前衛4/控え4チェック)
  - 状態: 完了
  - メモ:
    - `src/lib/types.ts` にデッキ関連の型を追記: `DeckCardInput`(`{characterId, role:
      'front'|'bench'}`)、`DeckInput`(`{name, ownerName?, cards}`、docs/設計.md 3章の
      リクエストボディ形状に対応)、`DeckSummary`(`id/name/ownerName/createdAt` の
      一覧APIレスポンスDTO)、`Deck`(`front`/`bench` 各4体の `Character`(パラメータ・
      必殺技含む全情報)配列を持つ詳細APIレスポンス形状)。
    - `src/lib/decks/validation.ts` を新規作成。zodで `deckCardSchema`
      (`characterId`: 正の整数、`role`: `'front'|'bench'` のenum)と
      `deckInputSchema`(`name`: 1文字以上・trim、`ownerName`: 任意、`cards`:
      `deckCardSchema` の配列で `.length(8, ...)` によりちょうど8件)を実装。
      `superRefine` で (1) `cards[].characterId` がすべて一意であること(`Set` の
      サイズ比較)、(2) `role === 'front'` がちょうど4件であること、(3)
      `role === 'bench'` がちょうど4件であることをそれぞれ検証し、違反時は
      `ctx.addIssue({ code: "custom", path: ["cards"], message: "..." })` で
      日本語メッセージのissueを発行する(キャラクター名タスク4の
      `characterInputSchema` と同じ `code: "custom"` の流儀に統一)。`characterId`
      の実在確認はDBアクセスが必要なため、このモジュールでは行わずリポジトリ層の
      責務とする設計にした(タスク説明の指示通り)。`validateDeckInput(input:
      unknown)` ヘルパーもキャラクター側と同じ形状(`{success:true,data}` /
      `{success:false,errors}`)で実装。
    - `src/lib/decks/repository.ts` を新規作成。`createDeck(input)` /
      `listDecks()` / `getDeckById(id)` / `updateDeck(id, input)` /
      `deleteDeck(id)` を実装。
      - `createDeck`/`updateDeck` はトランザクション内で先に
        `assertCharactersExist`(`cards[].characterId` を1件ずつ
        `SELECT 1 FROM characters WHERE id = ?` で存在確認、存在しなければ
        `CharacterNotFoundError`(`code: "CHARACTER_NOT_FOUND"`、
        `characterId` プロパティ付き、`lib/characters/repository.ts` の
        `CharacterInUseError` と同じ設計方針)を投げる)を呼び出してから
        `decks` へのINSERT/UPDATEと `deck_cards` の(再)挿入を行う。
        `updateDeck` は既存 `deck_cards` を `DELETE FROM deck_cards WHERE
        deck_id = ?` で全削除してから再挿入する(キャラクターの
        `replaceParametersAndMoves` と同じ「全削除→再挿入」方針)。
        `slot_order` は `cards` 配列を `role` でフィルタしたうえでの
        インデックス(0始まり、front/bench各グループ内での出現順)とした。
        いずれもトランザクション内で例外を投げるため、存在しない
        `characterId` が含まれる更新は自動的にロールバックされ、デッキは
        変更前の状態のまま残ることを確認済み(下記動作確認参照)。
      - `getDeckById`(および `createDeck`/`updateDeck` 内部で使う
        `findDeckById`)は `deck_cards` を `role, slot_order, id` 順に取得し、
        各行の `character_id` に対して `lib/characters/repository.ts` の
        `getCharacterById` をそのまま再利用してキャラクター全情報
        (パラメータ・必殺技含む)を取得し、`role` に応じて `front`/`bench`
        配列に振り分ける設計にした(SQLの単一JOINではなくN+1クエリになるが、
        既存の `assembleCharacter`(キャラクター側)と同じ「MVPの想定データ量
        では問題にならない」という判断を踏襲し、かつ `getCharacterById` を
        再利用することでパラメータ/必殺技の行→型変換ロジックの重複を避けた)。
      - `listDecks()` は `SELECT id, name, owner_name, created_at FROM decks`
        のみ取得しタスク説明どおりの概要DTO(`DeckSummary`)を返す。
      - `deleteDeck(id)` は対象が存在しなければ `false` を返す(呼び出し元は
        404)。加えて、タスク説明には明記されていないが `battles` テーブルの
        `deck_a_id`/`deck_b_id` 外部キーに `ON DELETE CASCADE` が設定されて
        いない(schema.sql参照)ため、対戦で参照中のデッキを無防備に削除すると
        `foreign_keys = ON` 環境下でSQLiteのFOREIGN KEY制約違反により
        素の例外(500相当)になってしまう懸念があった。`lib/characters/
        repository.ts` の `CharacterInUseError`(409)と同じ設計方針を踏襲し、
        `battles` に当該デッキを参照する行があれば `DeckInUseError`
        (`code: "DECK_IN_USE"`)を投げて409を返す防御的チェックを追加した
        (現時点ではタスク11未実装のため `battles` に行は存在せず実害はないが、
        将来のバトル実行API実装時に備えた安全策として整合性を優先した)。
    - `src/app/api/decks/route.ts`(先頭に `export const runtime = "nodejs";`)に
      `GET`(`listDecks()` をそのまま200で返す)/`POST`(JSON不正で400、
      `validateDeckInput` 失敗で400、`createDeck` が `CharacterNotFoundError`
      を投げた場合は400、成功時201)を実装。
    - `src/app/api/decks/[id]/route.ts`(同じく `runtime = "nodejs"`)に
      `GET`(`parseDeckId` でid不正時400、`getDeckById` が `null` なら404、
      あれば200で詳細を返す)/`PUT`(id不正・JSON不正・バリデーション失敗で400、
      `CharacterNotFoundError` 捕捉時400、`updateDeck` が `null`(対象なし)
      なら404、成功時200)/`DELETE`(id不正で400、`deleteDeck` が `false`
      なら404、`DeckInUseError` 捕捉時409、成功時204)を実装。id解析は
      `lib/characters` の `[id]/route.ts` と同じ `parseCharacterId` 相当の
      正規表現チェック(`parseDeckId`)を用いた。
    - 動作確認:
      - `npm run build`(Next.js本体のビルド、TypeScript型チェック含む)・
        `npm run lint` がともにエラーなく成功することを確認(既知の
        `next.config.ts` 経由の「Encountered unexpected file in NFT list」
        warningのみで、タスク5・6のメモに記載済みの既知事象。ビルド失敗ではない)。
      - `rm -f data/game.db && npm run db:migrate && npm run db:seed` 実行後、
        `npm run dev -- -p 3711` で実サーバーを起動し、以下をすべて `curl` で確認
        (検証後 `npm run db:seed` を再実行しシードデータを元の16体/2デッキの
        状態に戻し、devサーバーも停止して終了):
        - `GET /api/decks` → 200、シード済み2件が `DeckSummary` 形状
          (`id/name/ownerName/createdAt`)で返ることを確認。
        - `GET /api/decks/1` → 200、`front`/`bench` それぞれ4体、各キャラクターが
          `parameters`/`specialMoves` 全件を含む形状であることを確認。
          `GET /api/decks/9999` → 404。`GET /api/decks/abc` → 400(id不正)。
        - `POST /api/decks`(cards 7件、front4/bench3)→ 400
          (`too_small` + 「控え(bench)はちょうど4件指定してください」の両issue)。
        - `POST /api/decks`(characterId重複)→ 400
          (「同じキャラクターを同一デッキに複数枚登録することはできません」)。
        - `POST /api/decks`(front5件/bench3件)→ 400(front/bench両方のissue)。
        - `POST /api/decks`(存在しないcharacterId=9999を含む8件)→ 400
          (「キャラクター(id=9999)が見つかりません。」)。
        - `POST /api/decks`(不正なJSON文字列ボディ)→ 400。
        - `POST /api/decks`(name空文字)→ 400。
        - `POST /api/decks`(role不正値 `"reserve"`)→ 400
          (`path: ["cards",7,"role"]`)。
        - `POST /api/decks`(正常な8体・front4/bench4)→ 201、作成物(id=3)が
          `front`/`bench` 各4体の全情報付きで返ることを確認。
        - `PUT /api/decks/3`(front/benchの一部入れ替え・name/ownerName変更)→
          200、`GET` で確認すると `front`/`bench` のキャラクターID構成が
          更新後の内容に一致していることを確認。
        - `PUT /api/decks/3`(存在しないcharacterId=99999を含む)→ 400
          (`CharacterNotFoundError`)。この直後に `GET /api/decks/3` で
          デッキが更新前の状態のまま(nameが「壊れたデッキ」になっていない)
          であることを確認し、トランザクションによるロールバックが
          正しく機能していることを確認した。
        - `PUT /api/decks/9999`(存在しないデッキ)→ 404。
        - `DELETE /api/decks/3` → 204。再度 `DELETE /api/decks/3` → 404。
          `DELETE /api/decks/9999` → 404。`GET /api/decks` で一覧が
          シード済み2件に戻っていることを確認。
      - 上記いずれも設計書3章のステータスコード方針(作成201・詳細/一覧200・
        更新200・削除204・検証失敗400・未存在404)、およびタスク説明の
        「存在しないデッキ/キャラクター参照時404/400を適切に返す」
        (デッキ未存在→404、キャラクター参照不正→400)どおりであることを確認した。
    - 残課題: なし。次タスク(8. Claude API接続基盤実装)以降で `lib/battles/*` の
      実装時、`getDeckById` が返す `Deck`(front/bench各4体の全情報)を
      `lib/battles/prompt.ts` のプロンプト構築にそのまま利用できる想定。
      また `deleteDeck` の `DeckInUseError`(409)はタスク11(戦闘実行API実装)で
      `battles` に実データが入るようになった際にあらためて動作確認するとよい。

- [x] 8. Claude API接続基盤実装(システムプロンプト読み込み・クライアント初期化)
  - 状態: 完了
  - メモ:
    - `src/lib/claude/client.ts` を新規作成。モジュール読み込み時に `fs.readFileSync(path.resolve(process.cwd(),
      "システムプロンプト.md"), "utf-8")` を実行し、`SYSTEM_PROMPT` 定数としてキャッシュ(内容は一切改変しない。
      `lib/db/client.ts` の `DB_PATH` 解決と同じ `process.cwd()` 基準の流儀に合わせた)。
    - `@anthropic-ai/sdk`(`package.json` に `^0.110.0` としてタスク1で導入済み)の `Anthropic` クライアントを
      `new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })` で明示的に初期化してエクスポート
      (設計書1.4節の「公式SDKを使用」方針どおり)。
    - `MODEL` 定数を `process.env.ANTHROPIC_MODEL || "claude-opus-4-8"` として定義・エクスポート。
    - `callBattleJudge(userMessage: string): Promise<string>` をエクスポート。
      `client.messages.create({ model: MODEL, max_tokens: 8000, system: SYSTEM_PROMPT,
      messages: [{ role: "user", content: userMessage }] })` を呼び出し、
      `temperature`/`top_p`/`top_k`/`thinking` はいずれも指定しない(設計書1.4節・タスク説明のとおり)。
      返却された `response.content` 配列を `type === "text"` でフィルタし、各ブロックの `text` を連結して返す
      (`Anthropic.TextBlock` 型で絞り込み)。`try/catch` は行わず、Claude APIが投げる例外(認証エラー・
      レート制限等)はそのまま呼び出し元に伝播する。
    - 動作確認:
      - `npm run build`(Next.js本体のビルド、TypeScript型チェック含む)・`npm run lint` がともにエラーなく
        成功することを確認(既知の `next.config.ts` 経由の「Encountered unexpected file in NFT list」
        warningのみで、タスク5〜7のメモに記載済みの既知事象・ビルド失敗ではない)。
      - scratchpad配下に一時スクリプトを作成し `npx tsx` で実行し、`SYSTEM_PROMPT`(長さ237文字、
        「あなたは公平なバトルシミュレーターです。」で始まる内容)が `システムプロンプト.md` の内容と
        一致していること、`MODEL` が `claude-opus-4-8`(環境変数未設定時のデフォルト)であることを確認。
      - **重要な制約:** この開発サンドボックス環境には実際の `ANTHROPIC_API_KEY`(有効なAPIキー)が
        設定されておらず(`.env.local` も未作成)、`ant` CLIによる認証プロファイルも存在しなかった。
        ファイルシステム上でのAPIキー探索は行っていない(シークレットを推測・探索する行為はしない方針)。
        そのため「正常にテキストが返ること」の確認は本タスク内では完了できなかった。
        代わりに、`api.anthropic.com` への実際のネットワーク到達性(`curl` で401が返ることを確認)と、
        意図的に無効なAPIキーを使って `callBattleJudge` を呼び出し、Anthropic APIへの実HTTPリクエストが
        送信されること(実際の `request_id` を含む401レスポンスを受信)、SDKの `AuthenticationError`
        (`status: 401`)が `try/catch` で握りつぶされず呼び出し元にそのまま伝播することを確認した。
        これにより「クライアント初期化・システムプロンプト読み込み・API呼び出し配線・エラー伝播」は
        実際のAPIエンドポイントに対して検証済みだが、有効なAPIキーでの成功レスポンス(テキスト内容)の
        確認は未実施。
    - 残課題(2026-07-04 解消): 有効な `ANTHROPIC_API_KEY` は引き続き未取得だが、代わりに
      OpenRouter(無料枠モデル)経由でAI審判が実際に動作することを確認した。
      `lib/claude/client.ts` に `OPENROUTER_API_KEY` が設定されている場合は
      `openai` SDK(`baseURL: "https://openrouter.ai/api/v1"`)経由でOpenRouterを優先して
      呼び出す分岐を追加(未設定時は従来通りAnthropic経由、`ANTHROPIC_API_KEY`と排他)。
      モデルは `OPENROUTER_MODEL`(未設定時デフォルト `nvidia/nemotron-3-nano-30b-a3b:free`)。
      複数の無料モデル(`openai/gpt-oss-120b:free`, `meta-llama/llama-3.3-70b-instruct:free`,
      `qwen/qwen3-coder:free` 等)を実際にOpenRouter APIへ投げて比較した結果、多くは
      アップストリーム側の一時的な429(レート制限)で失敗し、大型の
      `nvidia/nemotron-3-super-120b-a12b:free` は応答こそ得られるが1リクエスト5分以上かかる
      うえchain-of-thoughtの序文("We need to...")がJSON本体の前に混入しパース失敗することが
      あった。`nvidia/nemotron-3-nano-30b-a3b:free` は約30〜40秒で完走し、日本語の
      システムプロンプト・ユーザーメッセージに対して自然な日本語で、かつ前置きなしの
      純粋なJSON(`responseSchema`検証も一発で通過)を安定して返したため、これをデフォルトに
      採用した。
      実機確認: `rm -f data/game.db && npm run db:migrate && npm run db:seed` 後、
      `npm run dev` でサーバーを起動し (1) `POST /api/battles`(`{"deckAId":1,"deckBId":2}`)を
      `curl` で実行し201・`status:"completed"`・`battleLog`/`events`/`result`(勝敗・MVP・
      `mvpCharacterId` 解決含む)が正しく返ることを確認、(2) Playwright(`chromium`
      ヘッドレス)で実ブラウザから `/battles` ページを操作し、デッキA/Bを選択して
      「対戦開始」をクリック→ローディング表示→`/battles/:id` へ遷移→戦闘前分析・
      ターンごとの戦闘ログ・必殺技演出カード・最終結果(勝者・MVP)が画面に表示される
      ところまで実際に確認した(コンソールエラーなし)。
      あわせて、この確認作業中に `README.md`(誤って貼り付けられたCline認証用の
      `vscode://` ディープリンク、アクセストークン/リフレッシュトークン含む)と
      `OpenRouter.md`(平文のAPIキー)にシークレットが混入しているのを発見し、
      README.mdは元の内容に復元、OpenRouter.mdは実キーを除去して`.env.local`参照に
      置き換えた(いずれも未コミット・未pushの段階で発見・修正済み)。
      `.env.example` に `OPENROUTER_API_KEY`/`OPENROUTER_MODEL` を追記し、`package.json` に
      `openai` パッケージを追加。

- [x] 9. バトルプロンプト生成ロジック実装
  - 状態: 完了
  - メモ:
    - `src/lib/battles/prompt.ts` を新規作成し `buildBattlePrompt(deckA: DeckDetail,
      deckB: DeckDetail): string` を実装。引数の型 `DeckDetail` は
      `export type DeckDetail = Deck;`(`@/lib/types` の `Deck` の別名)として定義した。
      `lib/decks/repository.ts` の `getDeckById` が返す `Deck`(front/bench各4体、
      パラメータ・必殺技を含む全情報)がそのまま渡せるようにするため、既存の型を
      再利用しつつタスク説明の関数シグネチャ名(`DeckDetail`)にも合わせた形にした。
    - 内部ヘルパーとして `formatParameters`(パラメータを「名前:値」の読点区切り列挙に
      整形)、`formatSpecialMoves`(必殺技を「技名/説明/演出テキスト」の番号付き列挙に
      整形。0件の場合は「(必殺技なし)」、description/flavorText が null または
      空文字の場合は「(説明未設定)」「(演出テキスト未設定)」にフォールバック)、
      `formatCharacter`(1体分を「名前・説明・パラメータ・必殺技」の形にまとめる)、
      `formatDeckSection`(1デッキ分を「【チームA/チームB】デッキ名 → 前衛4体 → 控え4体」
      の形にまとめる)を実装し、`buildBattlePrompt` 本体はこれらを組み合わせて以下の
      セクションを順に連結した単一の文字列を返す:
      1. 冒頭の導入文
      2. チームA(前衛4体・控え4体の名前/説明/パラメータ/必殺技)
      3. チームB(同上)
      4. 戦闘フェーズ(開発指示書.md「戦闘フェーズ」節の8段階をそのまま列挙:
         戦闘前分析→戦闘開始→通常行動→必殺技発動→戦闘不能判定→控え補充→勝敗決定→MVP発表)
      5. タスク説明で明記された5つの確定事項(行動順は完全ランダム、100ポイント制約は
         アプリ側で登録時に検証済みでAIは検証不要、必殺技の発動有無・使用技の選択は
         ランダム、前衛戦闘不能時は控えから自動的にランダム補充、勝敗はストーリー補正
         なく合理的に決定し理由を説明可能にする)を番号付きで明記
      6. 開発指示書.md「AI出力形式」節のJSONスキーマ(analysis/battleLog/events/result)を
         一字一句そのまま提示し、「以下のJSON形式のみを出力し、Markdownのコードブロックや
         説明文などは一切含めないこと。」を明示的に指示
    - このモジュール自身は100ポイント制約・8枚/前衛4/控え4制約の検証を一切行わない
      (それらは登録時に `lib/characters/validation.ts` / `lib/decks/validation.ts` で
      検証済みのデータのみが渡ってくる前提であることをファイル冒頭のコメントに明記)。
    - 動作確認:
      - `npm run build`(Next.js本体のビルド、TypeScript型チェック含む)・`npm run lint`
        がともにエラーなく成功することを確認(既知の `next.config.ts` 経由の
        「Encountered unexpected file in NFT list」warningのみで、タスク5〜8のメモに
        記載済みの既知事象・ビルド失敗ではない)。
      - `rm -f data/game.db && npm run db:migrate && npm run db:seed` 実行後、
        scratchpad配下に一時スクリプトを作成し `npx tsx` で実行し、
        `getDeckById(1)`/`getDeckById(2)`(シード済みサンプルデッキA/B)を
        `buildBattlePrompt` に渡した結果を目視確認(検証後スクリプトは削除済み、
        `npm run db:seed` を再実行しシードデータを元の状態に戻して終了):
        - チームA/チームBそれぞれ前衛4体・控え4体すべてが「名前・説明・パラメータ
          (例: `攻撃:38、防御:18、素早さ:24、体力:20`)・必殺技(例: `1. 技名:紅蓮斬 /
          説明:刀身を炎で覆い、前方の敵を薙ぎ払う。 / 演出テキスト:紅蓮の斬撃が闇を
          切り裂く!`、必殺技2件を持つキャラクターは2行とも出力される)」の形式で
          出力されていることを確認。
        - 戦闘フェーズ8段階・確定事項5項目・JSON出力形式の指示がすべて含まれていることを
          目視確認。JSON部分だけを取り出して `JSON.parse` したところエラーなくパースでき、
          開発指示書.md「AI出力形式」節のスキーマと完全一致していることを確認(`node -e`で検証)。
        - 戻り値が `typeof === "string"` の単一文字列であることを確認(シード済み
          デッキA/B同士で文字列長4228文字)。
        - パラメータ0件・必殺技0件・説明未設定(null)のダミーキャラクター(front/bench
          各4体)を持つ `Deck` オブジェクトを手組みして渡し、`formatParameters`
          が「(パラメータ未設定)」、`formatSpecialMoves` が「(必殺技なし)」、
          `formatCharacter` の説明部が「(説明未設定)」にそれぞれフォールバックし、
          例外を投げず正常に文字列が返ることを確認(境界値・欠損データケース)。
    - 残課題: なし。次タスク(10. AI応答パース&バリデーション実装)で
      `lib/battles/responseSchema.ts`・`lib/battles/parseResponse.ts` を実装した後、
      タスク11(戦闘実行API実装)で `buildBattlePrompt` → `callBattleJudge`
      (`lib/claude/client.ts`、タスク8で実装済み)→ `parseResponse` の一連のフローを
      `lib/battles/engine.ts` から呼び出す想定。

- [x] 10. AI応答パース&バリデーション実装(JSON抽出・リトライ)
  - 状態: 完了
  - メモ:
    - `src/lib/battles/responseSchema.ts` を新規作成。docs/設計.md 4章のスキーマを
      そのままzod化: `eventSchema = z.object({ type: z.string() }).passthrough()`
      (`character`/`effect`/`effectType`/`camera`/`message` 等の自由記述キーを許容)、
      `responseSchema = z.object({ analysis: {teamA, teamB, predictedWinner:
      z.enum(['teamA','teamB'])}, battleLog: z.array({turn: z.number(), message:
      z.string()}), events: z.array(eventSchema), result: {winner: z.enum([...]),
      mvp: z.string()} })`。`z.infer` から `BattleEvent`/`BattleAIResponse` 型も
      エクスポートした(`BattleAIResponse` は `generateBattleWithRetry` の戻り値の
      型として使用)。
    - `src/lib/battles/parseResponse.ts` を新規作成。
      - `extractJson(text: string): unknown`: `text.trim()` →先頭の
        `` ```json `` (大文字小文字問わず、言語指定は省略可)と末尾の `` ``` ``
        を正規表現(`/^```(?:json)?\s*/i`、`` /```\s*$/i ``)で除去→再度trim→
        `JSON.parse`。フェンスが無い場合はtrimのみ適用され、不正なJSON文字列の
        場合は `JSON.parse` がそのまま例外を投げる(呼び出し元で捕捉)。
      - `parseBattleResponse(text: string): { success: true; data:
        BattleAIResponse } | { success: false; error: string }`:
        `extractJson` を `try/catch` で実行し、例外時は
        `{ success: false, error: "AI応答をJSONとしてパースできませんでした: ..." }`
        を返す。パースできた場合は `responseSchema.safeParse()` を実行し、
        成功時 `{ success: true, data }`、失敗時
        `{ success: false, error: "AI応答が期待したJSON形式と一致しませんでした: ..." }`
        (zodのエラーメッセージを含む)を返す。
      - `class BattleGenerationError extends Error { rawText: string }` を実装
        (`lib/characters/repository.ts` の `CharacterInUseError` 等と同じ、
        識別可能な独自例外を投げる既存の設計方針を踏襲)。
      - `generateBattleWithRetry(userMessage: string): Promise<{ data:
        BattleAIResponse; rawText: string }>` を実装。`callBattleJudge`
        (`lib/claude/client.ts`、タスク8で実装済み)を呼び出し
        `parseBattleResponse` で検証、成功なら即座に `{ data, rawText }` を返す。
        失敗した場合のみ、元のユーザーメッセージ+直前の(1回目の)生テキスト+
        「前回の出力はJSONとして不正でした(理由: ...)。指定のJSON形式のみを
        出力し直してください。Markdownのコードブロックや説明文などは一切
        含めないこと。」という訂正指示を連結した1つの文字列で `callBattleJudge`
        を1回だけ再度呼び出す(`callBattleJudge` のシグネチャが単一の
        `userMessage: string` のみを受け取る設計のため、会話履歴の配列ではなく
        「直前の出力を引用した単一メッセージ」として訂正指示を構築する方式を
        採用した。docs/設計.md 1.4節の「直前のアシスタント発話+訂正メッセージを
        追加して再試行」という意図は、直前の出力をメッセージ本文に引用する形で
        実現している)。再試行後も失敗した場合は
        `BattleGenerationError`(2回目の生テキストを `rawText` に保持、
        `message` に1回目・2回目双方の失敗理由を含める)をthrowする。
    - 動作確認:
      - `npm run build`(Next.js本体のビルド、TypeScript型チェック含む)・
        `npm run lint` がともにエラーなく成功することを確認(既知の
        `next.config.ts` 経由の「Encountered unexpected file in NFT list」
        warningのみで、タスク5〜9のメモに記載済みの既知事象・ビルド失敗ではない)。
      - scratchpad配下に一時テストスクリプトを作成し `npx tsx` で実行し
        (`@/` importエイリアスは `tsx` からも問題なく解決されることを確認)、
        以下すべてを期待通りに検証(検証後スクリプトは削除済み):
        - `extractJson`: `` ```json ... ``` ``フェンス除去、`` ``` ... ``` ``
          (言語指定なし)フェンス除去、フェンスなし(trimのみ)、不正な文字列
          (`"not json at all"`)で例外がthrowされること。
        - `parseBattleResponse`: フェンス付きの正常なJSONで成功し `data` の
          中身(`result.mvp`等)が正しいこと、`events[]` に含めた
          `effectType`/`camera`/`message` 等の自由記述フィールドが
          `.passthrough()` により失われず残っていること、日本語文字列など
          JSONとして不正な入力で失敗しエラーメッセージに「パースできません
          でした」を含むこと、`analysis`等のフィールドが不足したJSONで
          スキーマ不一致により失敗しエラーメッセージに「期待したJSON形式」を
          含むこと、`predictedWinner`にenum外の値(`"teamC"`)を与えた場合も
          失敗すること。
        - `generateBattleWithRetry`: `client.messages.create`
          (`lib/claude/client.ts` がexportする `client`)を一時的にモック関数へ
          差し替えて検証。(1) 1回目が不正JSON・2回目が正常JSONを返すモックで
          `callBattleJudge`(実体は`client.messages.create`)がちょうど2回
          呼ばれ、2回目の`data`/`rawText`が返ること、(2) 1回目から正常JSONを
          返すモックで呼び出しが1回のみで完了すること、(3) 2回とも不正JSONを
          返すモックで`callBattleJudge`がちょうど2回呼ばれた後に
          `BattleGenerationError`がthrowされ、`rawText`に2回目の生テキストが
          保持されていること、をすべて確認。検証後はモックを元の関数に
          戻した(実際のネットワーク呼び出しは一切発生させていない)。
    - 残課題: なし。次タスク(11. 戦闘実行API実装)で `lib/battles/prompt.ts`
      (`buildBattlePrompt`)→ `generateBattleWithRetry` → 検証済み
      `BattleAIResponse` → DB永続化(`lib/battles/repository.ts`、
      `battles`/`battle_logs`/`battle_events` への保存、`BattleGenerationError`
      捕捉時は `status='failed'`・`error_message`・`rawText`(`raw_ai_response`)
      への保存)という一連のフローを `lib/battles/engine.ts` から呼び出す想定。

- [x] 11. 戦闘実行API実装(POST/GET /api/battles, GET /api/battles/:id)
  - 状態: 完了
  - メモ:
    - `src/lib/types.ts` にバトル関連の型を追記: `BattleStatus`
      (`'pending'|'running'|'completed'|'failed'`)、`BattleInput`
      (`{deckAId, deckBId}`)、`BattleAnalysis`/`BattleLogEntry`/
      `BattleEventDetail`(`{turn, type, character, effect, raw}`、`raw`に
      イベントオブジェクト全体を保持)/`BattleResult`、`BattleSummary`
      (一覧APIレスポンス概要DTO)、`BattleDetail`(docs/設計.md 3章の
      `BattleDetail`形状イメージと一字一句対応)を定義した。
    - `src/lib/battles/repository.ts` を新規作成。
      - `createPendingBattle(deckAId, deckBId)`: `battles`へ
        `status='pending'`でINSERTし`lastInsertRowid`を返す。
      - `saveBattleResult(battleId, parsed: BattleAIResponse, rawText)`:
        `db.transaction()`内で (1) `battles`を`status='completed'`・
        `analysis_team_a/b`・`predicted_winner`・`winner`・`mvp_name`・
        `mvp_character_id`・`raw_ai_response`・`completed_at=datetime('now')`
        でUPDATE、(2) `battle_logs`へ`battleLog[]`を`turn`/`message`/
        `sort_order`(配列index)で一括INSERT、(3) `battle_events`へ
        `events[]`を`event_type=type`/`character_name=character??null`/
        `effect=effect??null`/`raw_json=JSON.stringify(event)`/
        `turn`(イベントに数値`turn`キーがあればその値・無ければ`null`、
        `extractEventTurn`ヘルパーで`typeof === "number"`判定)/
        `sort_order`(配列index)で一括INSERT、を実行する。
      - `mvp_character_id`解決(`resolveMvpCharacterId`): `battles`行から
        `deck_a_id`/`deck_b_id`を取得し、`lib/decks/repository.ts`の
        `getDeckById`で両デッキのfront/bench計16体のキャラクター名を集め、
        `parsed.result.mvp`と`trim().toLowerCase()`で大小文字無視の完全一致を
        探索、ヒットしなければ`null`とする。
      - `markBattleFailed(battleId, errorMessage)`: `status='failed'`・
        `error_message`のみUPDATE(`completed_at`は更新しない、失敗は
        「完了」ではないため)。
      - `getBattleDetail(id)`: `battles`+`decks`(deckA/deckB名)をJOINする
        共通SQL(`SELECT_BATTLE_WITH_DECK_NAMES`)を`listBattles`とも共用し、
        `battle_logs`/`battle_events`をそれぞれ`sort_order ASC, id ASC`で
        取得して`BattleDetail`形状に組み立てる。`analysis`は
        `analysis_team_a/b`・`predicted_winner`がすべて非nullの場合のみ
        オブジェクトを返し(未完了時は`null`)、`result`も同様に`winner`・
        `mvp_name`が非nullの場合のみオブジェクトを返す。存在しない場合は
        `null`。
      - `listBattles()`: 同JOINを`ORDER BY b.id ASC`で取得し
        `BattleSummary`(`id/status/deckA/deckB/winner/mvpName/createdAt/
        completedAt`)の配列を返す(既存の`listCharacters`/`listDecks`と
        同じ「id昇順」の並び順に合わせた)。
    - `src/app/api/battles/route.ts`(`runtime="nodejs"`):
      - `GET`: `listBattles()`をそのまま200で返す。
      - `POST`: (1) JSONパース失敗で400、(2) `deckAId`/`deckBId`が
        いずれも正の整数でない場合400(`parseBattleInput`ヘルパー)、
        (3) `getDeckById`で両デッキの実在を確認(存在しなければ400、
        取得したデッキ詳細はそのまま(4)のプロンプト構築にも再利用し
        二重取得を避けた)、(4) `createPendingBattle`でpending行を作成、
        (5) `buildBattlePrompt`→`generateBattleWithRetry`を`try/catch`で
        実行し、成功時は`saveBattleResult`を呼んで201で
        `getBattleDetail(battleId)`を返す。失敗時(`BattleGenerationError`
        だけでなく、Claude API呼び出し自体の失敗(認証エラー等の
        `Anthropic`SDK例外)も含めて`catch`で捕捉)は`markBattleFailed`で
        `status='failed'`・`error_message`を保存し、502で
        `{ errorMessage }`を返す(docs/設計.md 3章
        「Claude呼び出し失敗時は502、詳細はerrorMessage」に対応)。
    - `src/app/api/battles/[id]/route.ts`(`runtime="nodejs"`)に`GET`を実装。
      `id`は`lib/decks`/`lib/characters`の`[id]/route.ts`と同じ正規表現
      (`/^\d+$/`)ベースの`parseBattleId`で検証(不正なら400)、
      `getBattleDetail`が`null`なら404、あれば200で詳細を返す。
    - 動作確認:
      - `npm run build`(Next.js本体のビルド、TypeScript型チェック含む)・
        `npm run lint`がともにエラーなく成功することを確認(既知の
        `next.config.ts`経由の「Encountered unexpected file in NFT list」
        warningのみで、タスク5〜10のメモに記載済みの既知事象・ビルド失敗
        ではない)。
      - zodの`.passthrough()`スキーマから推論される`BattleEvent`型は
        追加キー(`character`/`effect`/`turn`等)が`{}`型(値ありnull/undefined
        除外型)として推論され`string | null`等へ直接代入できないことを
        一時的な型チェック専用ファイル(`src/lib/battles/__typecheck_tmp.ts`、
        確認後削除済み)で確認したうえで、`(event.character ?? null) as
        string | null`という型アサーションを採用した(タスク説明の
        `event.character??null`という代入式をそのまま踏襲しつつ、
        better-sqlite3のbindパラメータ型と整合させるための最小限の
        アサーション)。
      - `rm -f data/game.db && npm run db:migrate && npm run db:seed`済みの
        状態(`characters`16件/`decks`2件/`battles`0件、シード済みデッキA=id1・
        デッキB=id2)で`npm run dev -- -p 3811`を起動し、以下をすべて`curl`で
        確認:
        - `GET /api/battles`(バトル0件時)→ 200、`[]`。
        - `POST /api/battles`(ボディ`{}`、deckAId/deckBId欠如)→ 400
          (「deckAId・deckBIdはいずれも正の整数で指定してください。」)。
        - `POST /api/battles`(`deckAId`が文字列`"1"`)→ 400(型不正)。
        - `POST /api/battles`(`deckBId=9999`、存在しないデッキ)→ 400
          (「デッキ(id=9999)が見つかりません。」)。
        - `POST /api/battles`(不正なJSON文字列ボディ)→ 400。
        - `GET /api/battles/abc`(id不正)→ 400。`GET /api/battles/9999`
          (未存在)→ 404。
        - `POST /api/battles`(`deckAId=1, deckBId=2`、実在デッキ)→
          この開発サンドボックスには有効な`ANTHROPIC_API_KEY`が設定されて
          いない(タスク8のメモと同じ既知の制約)ため、実際に
          `api.anthropic.com`へHTTPリクエストが送信され認証エラー
          (`Could not resolve authentication method...`)が返り、
          `generateBattleWithRetry`(内部の`callBattleJudge`)がその例外を
          そのままthrowし、`route.ts`の`catch`ブロックで捕捉→
          `markBattleFailed`実行→502で
          `{"errorMessage":"Could not resolve authentication method..."}`
          が返ることを確認。これにより「バリデーション→デッキ実在確認→
          pending行作成→プロンプト構築→Claude呼び出し→(失敗時)
          markBattleFailedへの分岐→502レスポンス」という失敗系の配線一式が
          実際のAPIエンドポイントに対して動作することを確認した
          (タスク8と同様、有効なAPIキーでの成功レスポンスの確認は
          本タスクでも未実施)。
        - 上記POST後、`battles`テーブルを直接確認し
          `status='failed'`・`error_message`に上記メッセージが保存され、
          `completed_at`が`null`のままであることを確認。
        - `GET /api/battles/1`(上記failedレコード)→ 200、
          `{id:1, status:"failed", deckA:{id:1,name:"サンプルデッキA"},
          deckB:{id:2,name:"サンプルデッキB"}, analysis:null, result:null,
          errorMessage:"...", battleLog:[], events:[]}`という
          `BattleDetail`形状であることを確認。
        - `GET /api/battles`(1件時)→ 200、対応する`BattleSummary`
          (`winner:null, mvpName:null`)が返ることを確認。
      - dev serverを停止後、`saveBattleResult`(成功系の保存フロー、
        Claude API呼び出しを伴わないためAPIキー無しでも検証可能)を
        scratchpad配下の一時スクリプト(`npx tsx`、確認後削除済み)経由で
        直接呼び出し、以下を確認:
        - キャラクターid=1(`紅蓮の剣士アレン`、デッキA前衛)の名前を
          一時的に`IceKnight`へSQLで直接変更したうえで、
          `mvp: "iceknight"`(小文字)を含む`BattleAIResponse`を
          `saveBattleResult`に渡し、`getBattleDetail`の
          `result.mvpCharacterId`が`1`に解決される(大小文字無視の完全一致)
          ことを確認。
        - `battleLog`2件・`events`2件(1件目は`turn:2`付きの
          `special_move`イベントで`camera`等の自由記述フィールドが
          `raw`にそのまま残ること、2件目は`turn`フィールドを含まない
          `narration`イベントで`turn`/`character`/`effect`がいずれも
          `null`になること)を確認。
        - `analysis`・`result`が`parsed`の内容通りに反映され、
          `status`が`pending`→`completed`に変わること、`listBattles()`の
          該当エントリが`BattleSummary`形状で一致することを確認。
        - `result.mvp`が両デッキのどのキャラクター名にも一致しない文字列の
          場合、`mvpCharacterId`が`null`になることを確認。
        - `markBattleFailed`を直接呼び出した場合も`status='failed'`・
          `errorMessage`が反映され、`analysis`/`result`が`null`のままに
          なることを確認。
        - 検証後、一時スクリプトを削除し`npm run db:seed`を実行して
          `characters`(16件、id=1の名前も`紅蓮の剣士アレン`へ復元)・
          `decks`(2件)・`battles`(0件)を元の状態に戻したことを確認。
      - `npm run build`・`npm run lint`を最終確認として再実行し、
        いずれもエラーなく成功することを確認。
    - 残課題: 実際に有効な`ANTHROPIC_API_KEY`を用いた成功系(`status=
      'completed'`まで到達するAPI経由でのフルフロー)の確認はタスク8と
      同様に未実施(このサンドボックス環境の制約による)。次タスク
      (12. フロントエンド共通レイアウト・ナビゲーション実装)以降で
      `POST /api/battles`をUIから呼び出す際、有効なAPIキーが設定された
      環境で改めて成功レスポンス(`201 BattleDetail`)を確認するとよい。

- [x] 12. フロントエンド共通レイアウト・ナビゲーション実装
  - 状態: 完了
  - メモ:
    - `src/app/layout.tsx`: `create-next-app`生成のデフォルト内容(`title: "Create Next App"`等)を、
      本アプリ用のメタデータ(`title: "AIキャラクターカードバトル"`、簡潔な`description`)に置き換え、
      `<html lang="ja">`に変更。`<body>`直下に`<Nav />`→`<main className="site-main"><div className="container">{children}</div></main>`
      →`<footer className="site-footer">`の共通構造を設置。既存の`Geist`/`Geist_Mono`フォント読み込みはそのまま維持。
    - `src/components/Nav.tsx`: 新規作成。トップ(`/`)・キャラクター(`/characters`)・デッキ(`/decks`)・
      対戦(`/battles`)への`next/link`によるナビゲーションリンクを持つヘッダー(サイトロゴ+`<nav>`)を実装。
      `docs/設計.md`のディレクトリ構成に合わせ、キャラクター/デッキ/対戦のトップレベルパスへリンクする
      (一覧・セットアップページ自体はタスク13〜15で実装するため、この時点では未実装で404になる)。
      インタラクティブな状態(アクティブリンクのハイライト等)は持たせず、シンプルなサーバーコンポーネントとした。
    - `src/app/globals.css`: `create-next-app`のデフォルト最小限CSSをベースに、
      (1) `--background`/`--foreground`に加え`--muted`/`--border`/`--surface`/`--accent`/`--accent-foreground`/
      `--header-background`のCSS変数を追加しライト/ダーク(`prefers-color-scheme`)両対応、
      (2) `h1`〜`h4`・`p`等の基本タイポグラフィ(サイズ・行間)、
      (3) 最大幅960pxの`.container`クラス(左右パディング付き、中央寄せ)、
      (4) ヘッダー(`.site-header`/`.site-header__inner`/`.site-logo`/`.site-nav`)・
      メイン(`.site-main`)・フッター(`.site-footer`)の共通レイアウトクラス、
      (5) トップページのCTA等で使う汎用パーツ(`.card`/`.button`/`.button-primary`/`.button-secondary`/`.button-group`)
      を追加。
    - `src/app/page.tsx`: `create-next-app`のデフォルト(Next.jsロゴ・Vercelリンク等)を全面的に置き換え、
      ゲームの簡単な説明(自由なキャラクター作成→デッキ編成→AI審判による対戦、という流れ)と、
      「キャラクター作成」(`/characters`, primaryボタン)・「デッキ編成」(`/decks`)・「対戦する」(`/battles`)
      への導線リンク(`next/link`、`.button`系クラスで装飾)、および3ステップの簡単な説明カードを実装。
    - 未使用となった`src/app/page.module.css`(CSS Modules版のデフォルトスタイル)は削除
      (grepで参照元がないことを確認済み)。トップページのスタイルは`globals.css`のクラス+インラインstyleのみで完結させた。
    - 動作確認: `npm run build`(型チェック含め成功、`/`が静的ページとして生成されることを確認)、
      `npm run lint`(エラーなし)。さらに`npm run dev`で起動し`curl`で`/`のHTMLを取得して
      ヘッダーナビゲーション・トップページ本文・フッターが期待通り出力されることを確認。
      あわせて`/characters`・`/decks`・`/battles`がこの時点では意図通り`404`になること
      (後続タスクで実装される想定であることと矛盾しない)を確認した。確認後は`next dev`プロセスを終了した。
    - 残課題・注意点: `/characters`・`/decks`・`/battles`のページ自体は本タスクでは未実装(タスク13〜15で実装予定)。
      Navのリンクラベルは日本語(トップ/キャラクター/デッキ/対戦)とし、トップページのボタン文言(キャラクター作成/
      デッキ編成/対戦する)とは表現を統一していない(ヘッダーは短い一覧名、トップページCTAはタスク名寄りの表現)が、
      リンク先パスはいずれも設計書通り`/characters`・`/decks`・`/battles`に統一している。
      なお`npm run build`時に`Turbopack build encountered 1 warnings`として`next.config.ts`経由の
      ファイルトレース警告(`lib/db/client.ts`のファイルシステム操作に起因)が出力されるが、これは本タスク以前
      から存在する既存の警告でありビルド自体は成功する(本タスクの変更とは無関係)。

- [x] 13. フロントエンド: キャラクター作成/一覧画面実装
  - 状態: 完了
  - メモ:
    - `src/app/characters/page.tsx`(サーバーコンポーネント)を新規作成。`GET /api/characters`
      を`cache: "no-store"`でfetchし常に最新の一覧を表示する。Next.jsのServer Componentから
      自分自身のRoute Handlerを実際のHTTP経由で呼び出すには絶対URLが必要なため、
      `next/headers`の`headers()`(Next.js 16では非同期。既存の`[id]/route.ts`が
      `await context.params`を使っているのと同じ「Next 15以降の動的APIは非同期」という
      流儀に合わせた)から`host`/`x-forwarded-proto`を読み取り`getBaseUrl()`で組み立てる
      ヘルパーを実装した。一覧は`CharacterSummary[]`(タスク5で定義済みの型、
      `parameterCount`ではなく`totalPoints`/`specialMoveCount`を使用)をカードのグリッド
      (`.character-grid`)で表示し、各カードに サムネイル画像(`imageUrl`があれば`<img>`、
      無ければ「No Image」のプレースホルダー)・名前・説明(あれば)・合計ポイント
      (`合計 {totalPoints}pt`)・必殺技数(`必殺技 {specialMoveCount}個`)をバッジ表示する。
      ページ上部に`/characters/new`への「+ 新しいキャラクターを作成」リンク(`.button-primary`)
      を設置。0件時は案内文のみ表示する分岐も実装した。
    - `src/app/characters/new/page.tsx`を`"use client"`のクライアントコンポーネントとして
      新規作成。フォーム状態(`name`/`description`/`imageUrl`/`parameters`/`specialMoves`/
      アップロード中・送信中・エラー各種)をすべて`useState`で保持する設計にした
      (`CharacterForm.tsx`は`docs/設計.md`のディレクトリ構成図には記載があるが、本タスクの
      説明文には登場せず`new/page.tsx`に直接フォームロジックを組み込む指示だったため、
      過剰実装を避け別コンポーネント化はしなかった)。
      - 画像アップロード欄: `<input type="file">`の`onChange`で選択ファイルを
        `FormData`に詰めて`POST /api/upload/image`へ送信し、成功時に返却された`url`を
        `imageUrl`ステートへ保持しつつ`<img>`でプレビュー表示(`画像を削除`ボタンで
        `imageUrl`を`null`に戻せる)。失敗時(400)は`{error}`のメッセージをそのまま
        画面に表示。同じファイルを再選択してもイベントが発火するよう送信後に
        `input.value = ""`でリセットする処理も入れた。
      - `src/components/PointAllocator.tsx`を新規作成。`CharacterParameterInput[]`
        (`@/lib/types`、既存の型をそのまま再利用)と`onChange`のみを受け取る完全制御
        コンポーネントとし、内部stateを持たない設計にした(状態は親の`new/page.tsx`が
        一元管理)。パラメーター名/値の行を`+ パラメーターを追加`/`削除`ボタンで動的に
        追加・削除でき(最後の1行は削除ボタンを`disabled`にして0件化を防止)、
        現在の合計・残りポイントをリアルタイム表示、合計が`MAX_TOTAL_POINTS`(100、
        `lib/characters/validation.ts`の`MAX_TOTAL_POINTS`と同じ値をこのコンポーネントから
        exportし親と共有)を超えた場合は合計表示を赤字化しつつ警告文(`.form-error`)を表示する。
      - `src/components/SpecialMoveEditor.tsx`を新規作成。`PointAllocator`と同じ「完全制御・
        内部stateなし」の方針で、技名/説明/演出テキストの行を動的に追加・削除できるUIを実装
        (0件でも許容、0件時は案内文を表示)。フォーム欄は空文字列で保持し、APIへ送信する際に
        空文字列→`undefined`へ変換する処理は呼び出し側(`new/page.tsx`)の責務とした。
      - 送信(`handleSubmit`): クライアント側で名前必須・合計100ポイント以下を簡易チェックした
        うえで、`parameters`/`specialMoves`の空行(名前未入力)を除外し、
        `description`/`imageUrl`/`specialMoves[].description`/`specialMoves[].flavorText`の
        空文字列を`undefined`に変換した`CharacterInput`(`@/lib/types`)を組み立て
        `POST /api/characters`へ送信。成功(201)時は`router.push("/characters")`で一覧へ
        リダイレクト(一覧ページは`cache: "no-store"`のためリダイレクト後は必ず最新データが
        表示される)。失敗時(400)は`{error, details}`(`lib/characters/validation.ts`の
        zod issuesの配列)を受け取り、`error`をバナー(`.form-error-banner`)、
        `details[].message`の一覧を`.form-error-list`として画面に表示する。
      - 送信ボタンは「送信中」「画像アップロード中」「名前未入力」「合計100ポイント超過」の
        いずれかに該当する場合`disabled`にする(タスク説明の「合計が100を超える場合は送信
        ボタンを無効化またはエラー表示する」を「無効化」側で対応、あわせて`PointAllocator`
        側でも警告テキストによる「エラー表示」を行い両対応とした)。
    - `src/app/globals.css`にタスク12で確立した「CSS変数+汎用クラス」のスタイル方針を踏襲した
      追加スタイルを実装(既存の`--background`/`--foreground`/`--muted`/`--border`/
      `--surface`/`--accent`等のCSS変数をそのまま利用、ライト/ダーク・`data-theme`属性の
      オーバーライドは既存定義のみで完結するため追加不要):`.badge`、`.page-header`、
      `input[type=text|number|file]`/`textarea`の共通スタイルとフォーカスリング、
      `.form-field`/`.form-error`/`.form-error-banner`/`.form-error-list`、
      `.character-image-preview`、`.character-grid`/`.character-card*`、
      `.point-allocator*`、`.special-move-editor*`、およびボタンの`disabled`時の
      見た目(`opacity: 0.5; cursor: not-allowed;`)。
    - 動作確認:
      - `npm run build`(Next.js本体のビルド、TypeScript型チェック含む)・`npm run lint`
        がともにエラーなく成功することを確認(`npm run lint`は警告も含め0件。`<img>`使用箇所
        には`@next/next/no-img-element`を明示的に`eslint-disable-next-line`コメント付きで
        許容し、ユーザーアップロード画像をそのまま表示する用途のため`next/image`は使わない
        判断を明記した)。ビルド結果のルート一覧で`/characters`が動的(`ƒ`、
        `cache: "no-store"`により想定通り)、`/characters/new`が静的(`○`、
        クライアントコンポーネントのみでサーバー側データ取得が無いため)であることを確認。
      - `npm run dev -- -p 3921`で実サーバーを起動し、以下を`curl`で確認
        (検証後、作成したテストキャラクター・アップロード画像は削除し`npm run db:seed`で
        シードデータ(16体/2デッキ/0バトル)を元の状態に戻し、devサーバーも停止して終了):
        - `GET /characters` → 200。シード済み16体すべてが`character-card`として描画され、
          各カードに「合計 &lt;value&gt;pt」「必殺技 &lt;count&gt;個」のバッジが
          期待通り出力されていることを確認(HTML内の`<!-- -->`はReactのテキストノード
          区切りコメントで、表示上は問題ないことも確認)。
        - `GET /characters/new` → 200。`point-allocator`/`special-move-editor`/
          `character-image`関連のクラス名やラベル(「キャラクター名」「パラメーター」
          「必殺技」)がHTMLに含まれることを確認。
        - `POST /api/upload/image`(1x1 PNG)→ 200で`{"url":"/uploads/characters/<uuid>.png"}`
          が返ることを確認。
        - その`url`を`imageUrl`に含めた`POST /api/characters`(名前・説明・パラメーター2件
          合計90・必殺技1件)→ 201で作成され、直後の`GET /characters`にその新規キャラクター
          (画像・名前・合計90pt・必殺技1個)が反映されていることを確認
          (`new/page.tsx`の`handleSubmit`が組み立てるJSON形状と同一のペイロードで検証)。
        - パラメーター合計120(100超過)のペイロードで`POST /api/characters`→ 400、
          `{"error":"入力内容に誤りがあります。","details":[{"path":["parameters"],
          "message":"パラメーターの合計は100ポイント以下にしてください"}]}`が返ることを確認
          (`new/page.tsx`の`fieldErrors`表示ロジックがそのまま`details[].message`を
          描画できる形状であることを確認)。
        - 検証後、作成したテストキャラクターを`DELETE /api/characters/:id`で削除、
          アップロードした画像ファイルを手動削除、`npm run db:seed`を再実行し
          `characters`=16件/`decks`=2件/`battles`=0件の元の状態に戻ったことを確認。
    - 残課題: キャラクター編集(更新)画面・個別詳細ページは本タスクの対象外(仕様上も
      一覧+作成のみが要求範囲)。次タスク(14. フロントエンド: デッキ編成画面実装)で
      `GET /api/characters`の一覧をデッキ編成の8枚選択UIから参照する想定。

- [x] 14. フロントエンド: デッキ編成画面実装
  - 状態: 完了
  - メモ:
    - `src/app/decks/page.tsx`(サーバーコンポーネント)を新規作成。`src/app/characters/page.tsx`
      (タスク13)と同じ方針で、`next/headers`の`headers()`から`host`/`x-forwarded-proto`を
      読み取り絶対URLを組み立て、`GET /api/decks`を`cache: "no-store"`でfetchして常に最新の
      一覧を表示する。`DeckSummary`(`id/name/ownerName/createdAt`)のみを扱う概要一覧のため、
      各デッキを`.deck-grid`のカードとして「デッキ名・作成者(`ownerName`が`null`の場合は
      『未設定』)・作成日時」を表示する。作成日時はSQLiteの`datetime('now')`形式
      (`"YYYY-MM-DD HH:MM:SS"`、UTC)をそのまま`new Date()`に渡すとタイムゾーン解釈が
      環境依存でブレるため、`formatDateTime()`ヘルパーで`"YYYY-MM-DDTHH:MM:SSZ"`
      (ISO 8601 UTC)に変換してから`toLocaleString("ja-JP", {...})`で整形する処理を追加した
      (パース失敗時は元の文字列をそのまま表示するフォールバック付き)。ページ上部に
      `/decks/new`への「+ 新しいデッキを編成」リンク(`.button-primary`)を設置。0件時は
      案内文のみ表示する分岐も実装した。
    - `src/components/DeckBuilder.tsx`を新規作成。`GET /api/characters`をクライアント側の
      `useEffect`内で`fetch`し(`cache: "no-store"`)、`CharacterSummary[]`を取得して
      チェックボックス付きの選択可能なリストとして表示する。`PointAllocator`/
      `SpecialMoveEditor`(タスク13)と同じ「完全制御コンポーネント」の方針を踏襲し、
      選択状態(`cards: DeckCardInput[]`)そのものは親(`decks/new/page.tsx`)が保持、
      このコンポーネントはキャラクター一覧取得に伴う読み込み中・取得失敗・0件の3状態のみを
      内部stateとして持つ。
      - 選択数の上限制御: `cards.length >= DECK_SIZE`(8)に達したら、未選択の
        チェックボックスをすべて`disabled`にすることで「ちょうど8体」を超える選択を
        UI側で防止する(すでに選択済みのチェックを外すことは常に可能)。
      - 前衛/控えトグル: 選択済みの各キャラクターの行に「前衛」「控え」の2つの
        トグルボタン(選択中の役割を`button-primary`、非選択を`button-secondary`で
        ハイライト)を表示し、クリックで`role`を切り替える。新規選択時のデフォルト
        `role`は「現在の前衛数が4未満なら`front`、そうでなければ`bench`」とし、
        素直に8体選べば概ね4/4に近づくようにした(最終的な役割はトグルで自由に調整可能)。
      - カウント表示: `.deck-builder__summary`に「選択中 n/8 体」「前衛 x/4・控え y/4」
        (タスク説明の文言どおり)を常時表示する。
      - `DECK_SIZE`/`FRONT_SIZE`/`BENCH_SIZE`は新規定数を作らず、タスク7で実装済みの
        `src/lib/decks/validation.ts`が既にexportしている同名の定数をそのまま re-export
        して使う設計にした(サーバー側バリデーションの「8枚・前衛4・控え4」としの数値が
        フロントエンドとズレることを防ぐため)。
    - `src/app/decks/new/page.tsx`を`"use client"`のクライアントコンポーネントとして
      新規作成(`characters/new/page.tsx`と同じ構成方針)。デッキ名(`name`、必須)・
      作成者名(`ownerName`、任意)の入力欄を配置し、`DeckBuilder`(`cards`/`onChange`)を
      組み込む。
      - 送信ボタンの無効化条件(`canSubmit`): 送信中でない・デッキ名が空でない・
        `frontCount === FRONT_SIZE(4) && benchCount === BENCH_SIZE(4)`のすべてを満たす
        場合のみ有効化する(タスク説明の「ちょうど4/4になっていない場合は送信ボタンを
        無効化する」に対応。`cards.length`が8であることは`frontCount`/`benchCount`が
        それぞれ4であれば自動的に満たされるため、`isRoleCountValid`のみで判定すれば
        十分と判断し、`DeckBuilder`側の選択数上限8と合わせて二重の安全策とした)。
      - 送信(`handleSubmit`): `{name: name.trim(), ownerName: ownerName.trim() ||
        undefined, cards}`(`DeckInput`、`@/lib/types`)を組み立てて`POST /api/decks`へ
        送信。成功(201)時は`router.push("/decks")`で一覧へリダイレクト。失敗時(400)は
        `{error, details}`(`lib/decks/validation.ts`のzod issuesの配列、`path: ["cards"]`の
        「前衛(front)はちょうど4件指定してください」等のメッセージを含む)を受け取り、
        `characters/new/page.tsx`と全く同じパターン(`error`をバナー`.form-error-banner`、
        `details[].message`一覧を`.form-error-list`)で画面に表示する。
    - `src/app/globals.css`にタスク12/13で確立した「CSS変数+汎用クラス」の方針を踏襲した
      追加スタイルを実装(新規CSS変数の追加は不要、既存の`--background`/`--muted`/
      `--border`/`--accent`等をそのまま利用):`.deck-grid`/`.deck-card*`
      (`character-grid`/`character-card*`と同系統のカードグリッド)、
      `.deck-builder__summary`/`.deck-builder__list`/`.deck-builder__item*`
      (選択済み行は`border-color: var(--accent)`でハイライト)/`.deck-builder__role-toggle`/
      `.deck-builder__role-button`。
    - 動作確認:
      - `npm run build`(Next.js本体のビルド、TypeScript型チェック含む)・`npm run lint`
        がともにエラーなく成功することを確認(`npm run lint`は警告も含め0件)。ビルド結果の
        ルート一覧で`/decks`が動的(`ƒ`、`cache: "no-store"`により想定通り)、`/decks/new`が
        静的(`○`、クライアントコンポーネントのみでサーバー側データ取得が無いため)である
        ことを確認(既知の`next.config.ts`経由の「Encountered unexpected file in NFT list」
        warningのみで、タスク5以降のメモに記載済みの既知事象・ビルド失敗ではない)。
      - `rm -f data/game.db && npm run db:migrate && npm run db:seed`実行後、
        `npm run dev -- -p 4021`で実サーバーを起動し、以下をすべて`curl`で確認
        (検証後、作成したテストデッキは削除し`npm run db:seed`でシードデータ
        (16体/2デッキ/0バトル)を元の状態に戻し、devサーバーも停止して終了):
        - `GET /decks` → 200。シード済み2件(サンプルデッキA/B)が`deck-card`として
          描画され、各カードに「作成者: seed」「作成日時: 2026/07/03 18:08」
          (`formatDateTime`によるJST表示)が期待通り出力されていることを確認
          (HTML内の`<!-- -->`はReactのテキストノード区切りコメントで表示上は
          問題ないことも確認、タスク13のメモと同じ既知の挙動)。
        - `GET /decks/new` → 200。「デッキ名」「作成者名」「キャラクター選択(ちょうど」
          等のラベル文言がHTMLに含まれることを確認。
        - `GET /api/characters` → 200、16件(`DeckBuilder`が実際に読み込む一覧と
          同一データ)であることを確認。
        - `POST /api/decks`(`new/page.tsx`が組み立てるものと同一形状のペイロード、
          8体・front4/bench4)→ 201で作成され(id=3)、`front`/`bench`各4体で
          返ることを確認。
        - `POST /api/decks`(front5/bench3の不正な構成)→ 400、
          `{"error":"入力内容に誤りがあります。","details":[{"path":["cards"],
          "message":"前衛(front)はちょうど4件指定してください"},{"path":["cards"],
          "message":"控え(bench)はちょうど4件指定してください"}]}`が返ることを確認
          (`new/page.tsx`の`fieldErrors`表示ロジックがそのまま`details[].message`を
          描画できる形状であることを確認)。
        - 検証後、作成したテストデッキ(id=3)を`DELETE /api/decks/3`で削除、
          `GET /api/decks`で一覧がシード済み2件(サンプルデッキA/B)に戻っている
          ことを確認、`npm run db:seed`を再実行して最終的な状態を確認。
      - この開発サンドボックス環境にはヘッドレスブラウザ(Playwright/Puppeteer等)が
        導入されていないため、チェックボックスのクリック操作や前衛/控えトグルボタンの
        実際のクリック挙動そのものをブラウザ経由では確認できなかった。代わりに
        コンポーネントのロジック(`handleToggleSelect`/`handleRoleChange`の状態遷移)を
        コードレビューで確認したうえで、`DeckBuilder`が送信する`cards`と全く同じ形状の
        ペイロードを`POST /api/decks`へ直接送信して正常系・異常系(front/bench不一致)
        双方がAPI側と整合することを確認する形で代替検証した。
    - 残課題: ヘッドレスブラウザでの実際のクリック操作確認は未実施(上記の代替検証で
      ロジック自体の妥当性は確認済み)。次タスク(15. フロントエンド: 対戦実行画面実装)で
      `GET /api/decks`の一覧を対戦セットアップ画面のデッキ選択UIから参照する想定。

- [x] 15. フロントエンド: 対戦実行画面実装
  - 状態: 完了
  - メモ:
    - `src/components/BattleSetupForm.tsx`を`"use client"`のクライアントコンポーネントとして
      新規作成。`src/components/DeckBuilder.tsx`(タスク14)と同じ「マウント時に`useEffect`で
      `GET /api/decks`(`cache: "no-store"`)を取得し、読み込み中/エラー/0件(実際は2件未満)の
      3状態を内部stateで管理する」方針を踏襲。取得した`DeckSummary[]`をデッキA/デッキBの
      2つの`<select>`プルダウン(未選択時は空の`value=""`option)として表示する。
      - 同一デッキ選択不可のバリデーション: `deckAId`/`deckBId`(ともに文字列state)が
        両方非空かつ一致する場合`isSameDeckSelected`を`true`とし、その旨のメッセージ
        (`.form-error`)を表示しつつ「対戦開始」ボタンを`disabled`にする(選択肢自体を
        フィルタして片方のプルダウンから選べなくする、のではなく「選べるが送信できない
        +エラー表示」という`characters/new`・`decks/new`と同じ「バリデーション失敗時は
        送信不可+エラー表示」方針に統一)。
      - 送信(`handleStartBattle`): `POST /api/battles`に`{deckAId: Number(deckAId),
        deckBId: Number(deckBId)}`(`BattleInput`、`@/lib/types`)を送信。送信中
        (`submitting=true`)はフォーム本体を隠し、代わりに`.battle-setup__loading`
        (CSSアニメーションのスピナー+「AIが対戦を実行しています。Claude APIの応答には
        数秒〜十数秒程度かかる場合があります。そのままお待ちください。」という案内文)を
        表示する(docs/設計.md 0章-4「戦闘実行は同期API呼び出しとする」「フロントエンドは
        ローディング状態を表示する」に対応)。
      - 成功時(201): レスポンスの`BattleDetail.id`を使い`router.push(`/battles/${battle.id}`)`
        で遷移する(タスク16で実装予定の`/battles/[id]`へのリンク。本タスク時点では
        該当ページ未実装のため遷移後は404になるが、タスク説明の要求はid遷移の実装までであり
        後続タスクで解消される想定)。
      - 失敗時(502、`{errorMessage}`。バリデーション不備等の400`{error}`も同様に処理):
        `errorMessage`/`error`いずれかの文字列を`.form-error-banner`に表示し、
        `submitting`を`false`に戻して選択状態(`deckAId`/`deckBId`)は保持したまま
        ボタンを再度有効化する。ボタンラベルは「エラー表示済みなら"再試行"、そうでなければ
        "対戦開始"」に切り替えることで、同じボタンをそのままクリックして再試行できるように
        した(`decks/new/page.tsx`の「送信エラー時もフォーム状態を保持する」方針を踏襲しつつ、
        タスク説明の「再試行できるようにする」をラベル変更で明示)。通信例外(`fetch`自体の
        失敗)も同じくキャッチしてエラーメッセージ表示+再試行可能にする。
    - `src/app/battles/page.tsx`(サーバーコンポーネント)を新規作成。`src/app/decks/page.tsx`
      (タスク14)と同じ`getBaseUrl()`/`formatDateTime()`ヘルパーをこのファイル内にも定義し
      (既存の`characters/page.tsx`・`decks/page.tsx`も同様にファイルごとに同じヘルパーを
      individually定義する既存方針を踏襲、共通化のための`lib`切り出しは本タスクの範囲外と判断)、
      `GET /api/battles`を`cache: "no-store"`で取得して`BattleSummary[]`を
      `id`降順(新しい対戦が上)に並べ替えて表示する。
      - 上段: `<h1>対戦</h1>`直下に`.card.battle-setup-section`で`BattleSetupForm`を配置
        (対戦セットアップ)。
      - 下段: 対戦履歴一覧(`.battle-history-list`)。各行を`next/link`の`<Link>`で
        `/battles/[id]`へのカードリンク(`.battle-history-item`)とし、「デッキA名 vs
        デッキB名」(`battle-history-item__matchup`)、勝敗バッジ(`formatOutcome`ヘルパー:
        `status==='completed'`かつ`winner`があれば「{勝者デッキ名} の勝利」、それ以外は
        `STATUS_LABELS`(pending→準備中/running→対戦中/completed→完了/failed→失敗)を表示)、
        日時(`formatDateTime(battle.createdAt)`)を表示する。0件時は案内文のみ表示する分岐も
        実装した。
    - `src/app/globals.css`にタスク12〜14で確立した「CSS変数+汎用クラス」の方針を踏襲した
      追加スタイルを実装(新規CSS変数の追加は不要):`select`(`input[type=text]`等と同系統の
      共通スタイル+フォーカスリング、`decks/new`の`DeckBuilder`では`<select>`を使っていな
      かったため今回新規追加)、`.battle-setup-section`/`.battle-setup__selects`/
      `.battle-setup__select-field`/`.battle-setup__vs`、`.battle-setup__loading`+
      `.battle-setup__spinner`(`@keyframes battle-setup-spin`によるCSSのみの回転スピナー、
      追加JS依存なし)、`.battle-history-list`/`.battle-history-item*`
      (`deck-card`と同系統のカードだが横並びレイアウト、hover時`border-color: var(--accent)`)。
    - 動作確認:
      - `npm run build`(Next.js本体のビルド、TypeScript型チェック含む)・`npm run lint`
        がともにエラーなく成功することを確認(`npm run lint`は警告も含め0件)。ビルド結果の
        ルート一覧で`/battles`が動的(`ƒ`、`cache: "no-store"`により想定通り)であることを
        確認(既知の`next.config.ts`経由の「Encountered unexpected file in NFT list」
        warningのみで、タスク5以降のメモに記載済みの既知事象・ビルド失敗ではない)。
      - `rm -f data/game.db && npm run db:migrate && npm run db:seed`実行後、
        `npm run dev -- -p 4131`で実サーバーを起動し、以下をすべて`curl`で確認(検証後
        `npm run db:seed`を再実行しシードデータ(16体/2デッキ/0バトル)を元の状態に戻し、
        devサーバーも停止して終了):
        - `GET /battles` → 200。SSR時点(`BattleSetupForm`の`useEffect`未実行)では
          「デッキ一覧を読み込み中...」が表示され、対戦履歴セクションには
          「まだ対戦履歴がありません。」の案内文が表示されることを確認。
        - `GET /api/decks` → 200、シード済み2件(サンプルデッキA=id1/サンプルデッキB=id2)を
          確認(`BattleSetupForm`がクライアント側で取得するのと同一データ)。
        - `POST /api/battles`(`deckAId:1, deckBId:2`、`BattleSetupForm`が送信するものと
          同一形状のペイロード)→ この開発サンドボックスには有効な`ANTHROPIC_API_KEY`が
          設定されていない(タスク8・11のメモと同じ既知の制約)ため502
          (`{"errorMessage":"Could not resolve authentication method..."}`)が返ることを
          確認(`BattleSetupForm`の失敗系分岐がこのレスポンス形状をそのまま処理できることを
          コードレビューで確認済み)。
        - 上記POST後、`GET /battles`のHTMLに対戦履歴として「サンプルデッキA vs
          サンプルデッキB」「失敗」バッジ・整形済み日時(`2026/07/03 18:18`のような表記)が
          表示され、当該行が`href="/battles/1"`の`<a>`タグになっていることを確認。
        - `GET /battles/1`(タスク16で実装予定、本タスク時点では未実装)→ 404であることを
          確認(意図通り。次タスクで解消される想定)。
      - この開発サンドボックス環境にはヘッドレスブラウザ(Playwright/Puppeteer等)が
        導入されていないため、タスク14と同様にプルダウン選択操作・ボタンクリックの実際の
        ブラウザ挙動そのものは確認できなかった。代わりに`BattleSetupForm`のロジック
        (`isSameDeckSelected`/`canSubmit`/`handleStartBattle`の状態遷移とAPIレスポンス
        分岐)をコードレビューで確認したうえで、フォームが送信するのと同一形状のペイロードを
        `POST /api/battles`へ直接送信し、成功系(`saveBattleResult`経由、Claude API呼び出しを
        伴わないため直接呼び出しで検証可能)・失敗系(502)双方がAPI側と整合することを
        タスク11の検証結果を踏まえて確認した。
      - また、有効な`ANTHROPIC_API_KEY`が無い制約により「`POST /api/battles`が201で成功し
        `router.push`で`/battles/[id]`へ実際に遷移する」というハッピーパスの
        エンドツーエンド確認は本タスクでも未実施(タスク8・11と同じ既知の制約。
        `router.push`の呼び出し自体はコードレビューで確認済み)。
    - 残課題: `/battles/[id]`ページ自体は次タスク(16. フロントエンド: バトルログ表示画面実装)
      で実装する(本タスク時点では対戦履歴・対戦開始成功後の遷移先はいずれも404)。
      ヘッドレスブラウザでの実際のクリック・プルダウン選択操作の確認、および有効なAPIキーでの
      成功系エンドツーエンド確認は未実施(上記の代替検証で妥当性は確認済み)。

- [x] 16. フロントエンド: バトルログ表示画面実装
  - 状態: 完了
  - メモ:
    - `src/app/battles/[id]/page.tsx`(サーバーコンポーネント)を新規作成。
      `src/app/battles/page.tsx`(タスク15)・`src/app/decks/page.tsx`(タスク14)と
      同じ方針(`headers()`から`host`/`x-forwarded-proto`を読み取り絶対URLを
      組み立てる`getBaseUrl()`、SQLiteの`datetime('now')`形式をJST表記へ整形する
      `formatDateTime()`をこのファイル内にも個別定義。既存タスクが「共通化のための
      `lib`切り出しは範囲外」と判断してきた方針をそのまま踏襲した)で
      `GET /api/battles/:id`を`cache: "no-store"`でfetchする`fetchBattle(id)`を実装。
      レスポンスが`404`(対戦未存在)または`400`(id不正、`/battles/abc`等の非数値パス
      パラメータ)の場合は`null`を返し、ページ側で`next/navigation`の`notFound()`を
      呼び出す(idが数値かどうかをこの層で正規表現チェックせず、API側のバリデーション
      結果にそのまま委ねる設計。400/404いずれも「表示できるものが無い」という意味では
      同じなので、両方とも404ページに寄せた)。それ以外の非2xxは`throw new Error(...)`
      でNext.jsの最寄りのエラーバウンダリに委ねる(`battles/page.tsx`の
      `fetchBattles`と同じパターン)。
    - ページ本体の構成(上から順に): (1) `.page-header`に対戦カード
      (`{deckA.name} vs {deckB.name}`見出し)+「対戦一覧に戻る」リンク、
      (2) 実行日時(`formatDateTime`)・ステータスバッジ(`STATUS_LABELS`、
      `battles/page.tsx`と同じ4状態のラベル定義をこのファイル内にも個別定義)、
      (3) `status==='failed'`の場合のみ`.form-error-banner`で`errorMessage`
      (`characters/new`・`decks/new`・`BattleSetupForm`と同じエラーバナー
      クラスを再利用)を表示、(4) `status==='pending'|'running'`の場合のみ
      「まだ処理中です」旨の案内カードを表示(タスク説明の「通常は同期APIのため
      発生しない想定だが念のため」の分岐)、(5) `battle.analysis`が非nullの場合のみ
      戦闘前分析セクション(デッキA/デッキBそれぞれの`teamA`/`teamB`テキストを
      並べ、`predictedWinner`(`'teamA'|'teamB'`)を対応するデッキ名に変換する
      `teamName()`ヘルパーで「事前予想: {デッキ名} が優勢」を表示)、
      (6) 戦闘ログセクション(`BattleLogViewer`に`battle.battleLog`をそのまま渡す)、
      (7) `battle.result`が非nullの場合のみ最終結果バナー(`teamName(result.winner)`
      で勝者デッキ名を解決した「{デッキ名} の勝利!」+`MVP: {result.mvpName}`)。
      `status='failed'`/`pending`/`running`時は`analysis`/`result`がいずれも
      `null`(`lib/battles/repository.ts`の`getBattleDetail`が非nullを返すのは
      全フィールドが揃っている場合のみ)のため、対応するセクション自体が
      自動的に非表示になる(余分な条件分岐を追加せずタスク説明の要求
      「failed時はerrorMessageを表示」「pending/running時の表示も考慮」を
      自然に満たす設計)。
    - `src/components/BattleLogViewer.tsx`を新規作成(`"use client"`)。
      `BattleDetail.battleLog`(`{turn, message}[]`、DB取得時点で
      `sort_order ASC` = AI出力順)を受け取り、まず`groupByTurn()`ヘルパーで
      「同じ`turn`値が連続する区間」ごとに1ブロックへグルーピングする
      (AIの1ターンに複数の実況メッセージが含まれるケースを1つの
      ターンブロックとしてまとめて表示するため。ターン番号が仮に
      非連続でもグルーピング自体は成立するようconsecutive-run方式にした)。
      タスク説明の「ワンクリックで次のターンを表示していくステップ式UI」を
      採用: `revealedCount`(初期値は先頭ターンのみ`1`、ターン0件なら`0`)を
      `useState`で保持し、「次のターンを見る」ボタンで1ターンずつ
      `revealedCount`をインクリメントする。あわせて「すべて表示」ボタン
      (一気に全ターン開示)、残りターン数の表示(`残り N ターン`)、
      全開示後は「すべてのターンを表示しました。」という案内文への切り替え、
      `battleLog`が0件の場合は「戦闘ログがありません。」を表示する分岐も実装した。
    - `src/app/globals.css`にタスク12〜15で確立した「CSS変数+汎用クラス」の
      方針を踏襲した追加スタイルを実装(新規CSS変数の追加は不要、既存の
      `--background`/`--muted`/`--border`/`--accent`/`--surface`等を
      そのまま利用):`.battle-detail__meta`/`.battle-detail__pending`、
      `.battle-detail__analysis*`(デッキA/B並列表示のグリッド)、
      `.battle-log__turns`/`.battle-log__turn*`(ターンブロックのカード)/
      `.battle-log__controls`/`.battle-log__remaining`/`.battle-log__done`、
      `.battle-result-banner*`(中央寄せ・`--accent`枠線の目立つバナー)。
    - 動作確認:
      - `npm run build`(Next.js本体のビルド、TypeScript型チェック含む)・
        `npm run lint`がともにエラーなく成功することを確認(`npm run lint`は
        警告も含め0件)。ビルド結果のルート一覧に`/battles/[id]`が動的
        (`ƒ`、`cache: "no-store"`により想定通り)として追加されていることを
        確認(既知の`next.config.ts`経由の「Encountered unexpected file in
        NFT list」warningのみで、タスク5以降のメモに記載済みの既知事象・
        ビルド失敗ではない)。
      - `rm -f data/game.db && npm run db:migrate && npm run db:seed`実行後、
        `lib/battles/repository.ts`の`createPendingBattle`/`saveBattleResult`/
        `markBattleFailed`をscratchpad配下の一時スクリプト(`npx tsx`、
        確認後削除済み)から直接呼び出し(有効な`ANTHROPIC_API_KEY`が
        このサンドボックスに無いため、タスク11・15と同様にClaude API呼び出しを
        経由しない直接投入で検証)、以下3件のテスト対戦データを作成した:
        (1) id=1: `status='completed'`、`battleLog`4件(ターン1に2件・
        ターン2に1件・ターン3に1件のメッセージ)、`analysis`・`result`
        (winner=teamA, mvp="紅蓮の剣士アレン")付き。(2) id=2: `status='failed'`、
        `errorMessage`付き。(3) id=3: `status='pending'`(未更新のまま放置)。
      - `npm run dev -- -p 4231`で実サーバーを起動し、以下をすべて`curl`で確認
        (検証後、作成したテスト対戦は`npm run db:seed`再実行により削除し
        `characters`16件/`decks`2件/`battles`0件の元の状態に戻し、devサーバーも
        停止して終了):
        - `GET /battles/1` → 200。「サンプルデッキA vs サンプルデッキB」見出し、
          ステータスバッジ「完了」、戦闘前分析セクション(デッキA/B双方の
          テキスト+「事前予想: サンプルデッキA が優勢」)、戦闘ログセクションで
          ターン1のブロックのみが初期表示され2件のメッセージがまとめて
          表示されていること(`battle-log__turn`が1個のみ、
          `battle-log__message`が2個)、「次のターンを見る」「すべて表示」
          ボタンと「残り 2 ターン」の表示、最終結果バナー
          (「サンプルデッキA の勝利!」「MVP: 紅蓮の剣士アレン」)が
          すべて期待通りHTMLに出力されていることを確認(ターングルーピング
          ロジックが意図通り機能していることを確認)。
        - `GET /battles/2`(failed)→ 200。`.form-error-banner`に
          「対戦の実行に失敗しました: AI応答をJSONとしてパースできませんでした:
          テスト用エラー」が表示され、戦闘前分析・最終結果バナーは
          いずれも表示されず(`analysis`/`result`が`null`のため)、
          戦闘ログセクションには「戦闘ログがありません。」が表示される
          ことを確認。
        - `GET /battles/3`(pending)→ 200。「この対戦はまだ処理中です。」の
          案内カードが表示され、エラーバナー・戦闘前分析・最終結果バナーは
          いずれも表示されないことを確認。
        - `GET /battles/9999`(存在しない対戦)→ 404。`GET /battles/abc`
          (id不正)→ 404。いずれもNext.jsの`not-found`ページが表示される
          ことを確認(`notFound()`の呼び出しが期待通り機能していることを確認)。
      - この開発サンドボックス環境にはヘッドレスブラウザ(Playwright/
        Puppeteer等)が導入されていないため、タスク14・15と同様に
        「次のターンを見る」「すべて表示」ボタンの実際のクリック操作
        そのものはブラウザ経由では確認できなかった。代わりに
        `BattleLogViewer`の状態遷移ロジック(`groupByTurn`のグルーピング
        結果・`revealedCount`の初期値/インクリメント/一括表示)をコード
        レビューで確認し、SSR時点(クリック前)のHTML出力
        (初期表示が先頭ターンのみであること、残りターン数の表示)が
        期待通りであることを`curl`で確認する形で代替検証した。
      - また、有効な`ANTHROPIC_API_KEY`が無い制約により「`POST /api/battles`
        (`BattleSetupForm`、タスク15)が実際に201で成功し、`router.push`で
        遷移した先の`/battles/[id]`が正常に描画される」というAPI経由の
        フルE2E確認はタスク8・11・15と同じ理由で本タスクでも未実施
        (`saveBattleResult`を直接呼び出す代替検証で`BattleDetail`の
        レンダリング自体は確認済み)。
    - 残課題: `battle.events`(必殺技演出イベント)は本タスクでは表示しない
      (`docs/設計.md`ディレクトリ構成図の`BattleEventCard.tsx`は次タスクの
      担当範囲と判断)。次タスク(17. 必殺技演出イベントの簡易表示実装)で
      `src/components/BattleEventCard.tsx`を実装し、この
      `src/app/battles/[id]/page.tsx`(または`BattleLogViewer`)に
      `battle.events`の表示を組み込む想定。ヘッドレスブラウザでの実際の
      クリック操作確認・有効なAPIキーでのフルE2E確認は上記の代替検証で
      妥当性は確認済みだが未実施のまま。

- [x] 17. 必殺技演出イベントの簡易表示実装
  - 状態: 完了
  - メモ:
    - `src/components/BattleEventCard.tsx` を新規作成。`props.event` は
      `@/lib/types` の `BattleEventDetail`(`{turn, type, character, effect, raw}`、
      タスク11で定義済みの型をそのまま再利用。タスク説明の受け取り形状と一致するため
      新規に型を定義し直さなかった)を1件受け取る。
      - `character`(必殺技使用キャラクター名、`null`時は「(使用キャラクター不明)」)・
        `effect`(演出内容、`null`時は「(演出内容の記述なし)」)を専用の見出し/本文
        として表示し、ヘッダーに「必殺技演出」バッジと(`event.turn`が非nullの場合のみ)
        「ターン N」を表示する。
      - `raw`(イベントオブジェクト全体)から `type`/`character`/`effect`
        (`DISPLAYED_KEYS`という固定Setで除外)以外の全キーを`Object.entries`で
        抽出し、`<dl>`で「キー: 値」形式(`<dt>キー</dt><dd>値</dd>`)で汎用的に
        列挙する設計にした。`formatExtraValue`ヘルパーで値の型に応じて
        文字列(空文字は「(空文字)」)/数値/真偽値はそのまま、`null`/`undefined`は
        「(未設定)」、オブジェクト/配列は`JSON.stringify`にフォールバックする
        (`effectType`/`camera`/`message`等の既知フィールドだけでなく、AIが
        返しうる任意の未知キー・ネストしたオブジェクト・`null`値でも例外を投げず
        表示できることを想定した設計)。
      - 見た目は通常の戦闘ログ(`.battle-log__turn`、`var(--background)`背景・
        `var(--border)`枠線)と明確に区別するため、`.battle-event-card`に
        `var(--accent)`の太め(2px)の枠線+`var(--surface)`背景を設定した上で、
        `@keyframes battle-event-flash`(マウント時に背景色が`var(--accent)`から
        `var(--surface)`へ0.6秒で収束する短いフラッシュ、1回のみ)と
        `@keyframes battle-event-pulse`(`box-shadow`が0〜12pxで2.4秒周期の
        `infinite`ループする控えめなパルス発光、フラッシュ終了と同時に開始するよう
        `0.6s`の`animation-delay`を設定)の2つのCSSアニメーションを付与した
        (`src/app/globals.css`に追記。既存のCSS変数`--accent`/`--surface`/
        `--border`/`--muted`/`--accent-foreground`をそのまま利用し新規変数追加は
        不要、タスク15の`@keyframes battle-setup-spin`と同じ「CSSのみで完結する
        アニメーション、追加JS依存なし」という既存方針を踏襲)。
    - `src/components/BattleLogViewer.tsx`(タスク16)を拡張し、`events?:
      BattleEventDetail[]`(省略時は`[]`)を新たに受け取れるようにした。
      - `useMemo`で `battleLog`由来の`turns`(`groupByTurn`の結果、既存ロジック
        そのまま)に含まれるターン番号の集合(`knownTurns`)を求め、`events`を
        (1) `event.turn`が`knownTurns`に含まれる数値の場合は`turnEventsMap`
        (`Map<number, BattleEventDetail[]>`)へ、(2) それ以外(`turn`が`null`、
        または`battleLog`のどのターンにも一致しない数値)は`tailEvents`配列へ
        振り分ける2グループに分類する。
      - ターンブロックの描画(`visibleTurns.map`)時、各ブロックの直後に
        `turnEventsMap.get(group.turn)`のイベントを`BattleEventCard`として
        差し込む。同じターン番号のブロックが理論上複数回出現しても(`groupByTurn`は
        連続区間ごとにグループ化するため通常は起こらないが、防御的に)
        `renderedEventTurns`という`Set`で「最初に出現したブロックの直後にのみ
        表示する」ようにし、重複表示を防いだ。ステップ式のターン開示
        (`revealedCount`)とも自然に整合し、該当ターンが未開示の間は
        そのターンに紐づくイベントも表示されない(タスク16の既存ステップUIを
        壊さない)。
      - `tailEvents`(`turn`が存在しない、または`battleLog`のどのターンにも
        一致しないイベント)は、ターン開示状況に関わらず常にログ末尾の
        「演出」セクション(`.battle-log__tail-events`、見出し`演出`)にまとめて
        表示する(タスク説明の「存在しない場合はログ末尾の『演出』セクションに
        まとめて」に対応。「存在しない」を文字通り`turn === null`のケースに加え、
        `battleLog`側に対応するターンが実在しないケースも同様に「紐付け先が
        無い」とみなして同じ末尾セクションに寄せる設計とした)。
      - `battleLog`が0件の場合(「戦闘ログがありません。」表示)でも
        `tailEvents`があれば末尾セクションは独立して表示されるよう、
        早期returnを廃止し `turns.length === 0` の分岐をJSX内の条件分岐に
        変更した。
    - `src/app/battles/[id]/page.tsx`(タスク16)を1行変更し、
      `<BattleLogViewer entries={battle.battleLog} />` を
      `<BattleLogViewer entries={battle.battleLog} events={battle.events} />`
      に変更(`BattleDetail.events`、タスク11で定義済み・`GET /api/battles/:id`
      が返す`events[]`をそのまま渡すのみ)。
    - `src/app/globals.css`にタスク12〜16で確立した「CSS変数+汎用クラス」の
      方針を踏襲した追加スタイルを実装(新規CSS変数の追加は不要、既存の
      `--accent`/`--surface`/`--border`/`--muted`/`--accent-foreground`を
      そのまま利用):`.battle-log__turn-events`(ターン直後のイベントカード縦積み)、
      `.battle-log__tail-events`/`.battle-log__tail-events-heading`(末尾「演出」
      セクション、`border-top: 1px dashed`で通常ログと視覚的に区切る)、
      `.battle-event-card*`(`__header`/`__badge`/`__turn`/`__character`/
      `__effect`/`__extra`/`__extra-row`)、`@keyframes battle-event-flash`/
      `@keyframes battle-event-pulse`。
    - 動作確認:
      - `npm run build`(Next.js本体のビルド、TypeScript型チェック含む)・
        `npm run lint`がともにエラーなく成功することを確認(`npm run lint`は
        警告も含め0件。既知の`next.config.ts`経由の「Encountered unexpected
        file in NFT list」warningのみで、タスク5以降のメモに記載済みの
        既知事象・ビルド失敗ではない)。
      - `rm -f data/game.db && npm run db:migrate && npm run db:seed`実行後、
        `lib/battles/repository.ts`の`createPendingBattle`/`saveBattleResult`を
        scratchpad配下の一時スクリプト(`npx tsx`、確認後削除済み)から直接
        呼び出し(有効な`ANTHROPIC_API_KEY`がこのサンドボックスに無いため、
        タスク11・16と同様にClaude API呼び出しを経由しない直接投入で検証)、
        以下2件のテスト対戦データを作成し`npm run dev -- -p 4321`で
        実サーバーを起動して`curl`で確認した(検証後、作成したテスト対戦は
        `npm run db:seed`再実行により削除し`characters`16件/`decks`2件/
        `battles`0件の元の状態に戻し、devサーバーも停止して終了):
        - id=1: `battleLog`がターン1〜3、`events`3件
          (①`turn:2`・`character:"紅蓮の剣士アレン"`・`effectType`/`camera`/
          `message`付き、②`turn`キー自体が無い`narration`イベント、
          ③`turn:99`(`battleLog`に存在しないターン番号)・`unknownField`
          (ネストしたオブジェクト)・`nullField:null`付き)。
          `GET /battles/1`のSSR初期HTML(ターン1のみ開示、`revealedCount=1`)を
          確認したところ、②③(`turn`が`null`/未知の99)の2件は開示状況に
          関わらず末尾「演出」セクションに表示され(`battle-log__tail-events`、
          `③`の`unknownField`が`{"nested":true,"value":42}`という文字列に、
          `nullField`が「(未設定)」に整形されて`<dt>キー</dt><dd>値</dd>`形式で
          列挙されていることを確認)、①(`turn:2`)はターン2ブロックが
          まだ開示されていないため初期HTMLには出現しない(意図通り)ことを
          `grep`で確認した。
        - id=2: `battleLog`がターン1のみ、`events`1件(`turn:1`・
          `character:"紅蓮の剣士アレン"`)。`GET /battles/2`で、ターン1ブロック
          (デフォルトで開示済み)の直後に`BattleEventCard`(「紅蓮の剣士アレン」
          「初手の必殺技!」)が表示され、末尾「演出」セクションは
          (該当イベントが無いため)出力されないことを確認した。
          これにより「`event.turn`が該当ターンのブロック直後に差し込まれる」
          経路も実際にレンダリングされることを確認できた。
      - 上記2件のテストで「ターン一致→該当ブロック直後」「turn無し/ターン
        不一致→末尾演出セクション」「未知キー・ネストオブジェクト・null値を
        含む`raw`でも例外を投げずキー: 値形式で列挙される」の3点をすべて
        実際のHTML出力で確認した。
    - 残課題: ヘッドレスブラウザ(Playwright/Puppeteer等)がこのサンドボックス
      環境に導入されていないため、フラッシュ/パルスアニメーションの実際の
      視覚的な動作(CSSアニメーションの見た目そのもの)はブラウザ経由では
      確認できていない(`@keyframes`の構文自体は`npm run build`が成功して
      いることと、CSSの静的レビューで妥当性を確認済み)。これで
      設計書5章の全17タスクが完了。

- [x] 18. DBをSQLiteからMySQL(PHPブリッジ経由)へ移行、静的アセットを外部ホストへ移設
  - 状態: 完了
  - メモ:
    - 背景: Render等Next.js稼働先のディスクが一時的で、DB(`data/game.db`)や
      アップロード画像が再デプロイ時に消える問題が繰り返し発生していた
      (タスク1・6・進捗冒頭のコミット履歴参照)。ユーザーの指示により、DBを
      MySQLに、`public/`配下の静的アセット(backgrounds/battle-backgrounds/
      characters/sound/uploads)をスターサーバー(`ss181301.stars.ne.jp`)へ
      移設し、永続化問題を恒久的に解決する方針とした。Next.js本体は引き続き
      Render等で稼働し続ける前提(今回移行しない)。
    - スターサーバー等の共有ホスティングは外部からのMySQL直接接続を許可しない
      ことが多いため、`public_html/card_game/php` に配置するPHPスクリプト
      (リポジトリ直下 `php/`)がMySQLへのアクセスを仲介するAPIブリッジとして
      働く設計にした。Next.js側は `src/lib/bridge/client.ts`(`callBridge`/
      `callBridgeUpload`、`X-API-Key`ヘッダー認証、PHP側の`{error,code,...}`を
      `BridgeError`として再構築)経由でHTTP越しに呼び出すのみで、SQLを直接
      実行することは無い。
    - `php/` の構成: `config.example.php`(DB接続情報・`API_KEY`/`ADMIN_KEY`/
      `ASSET_BASE_URL`の雛形、`config.php`自体は`.gitignore`対象)、`db.php`
      (PDOシングルトン、接続時に`SET time_zone='+00:00'`を実行しSQLite時代と
      同じUTC保存を維持)、`lib/response.php`・`lib/auth.php`(共通JSON応答・
      認証ヘルパー。`migrate.php`/`seed.php`は全データ削除を伴う破壊的操作の
      ため、通常CRUD用`API_KEY`とは別の`ADMIN_KEY`で保護する)、`lib/characters.php`
      (`characters.php`/`decks.php`共有のキャラクター組み立てヘルパー)、
      `schema.sql`(SQLite版DDLをMySQLへ翻訳: `AUTO_INCREMENT`、テーブルレベル
      `FOREIGN KEY`、`ENUM`、`CREATE INDEX IF NOT EXISTS`非対応を避けるため
      インデックスをテーブル定義内`KEY`として埋め込み)、`migrate.php`(冪等に
      schema.sql を分割実行)、`seed.php`(`seed-data.json`——`seed.ts`の
      CHARACTERS/DECKS配列リテラルを`node -e`で機械的に抽出したもの、手動転記
      によるミスを避けるため——を読み込み投入)、`characters.php`/`decks.php`/
      `battles.php`(各CRUD、既存の`db.transaction()`境界をそのまま1エンドポイント
      =1トランザクションとして実装)、`upload.php`(multipart受信・finfoによる
      実体MIME判定・2MB上限検証・ランダム16バイト16進ファイル名で
      `public_html/card_game/public/uploads/characters/`に保存)。
    - `saveBattleResult`が本来`decks`/`characters`両リポジトリを跨いで
      MVP名→キャラクターID解決(`resolveMvpCharacterId`)を行っていた点は、
      `battles.php`が`decks.php`相当のロジックをHTTP越しに呼び出す設計は
      事故のもとになるため、TypeScript側(`lib/battles/repository.ts`)に
      そのまま残し、解決済みの`mvpCharacterId`のみを`battles.php`の
      `save-result`アクションへプレーンな値として渡す設計にした。
    - `lib/characters/repository.ts`・`lib/decks/repository.ts`・
      `lib/battles/repository.ts`はエクスポート関数名・シグネチャを変えずに
      全て`callBridge`呼び出しへ置き換え、非同期(`Promise`)化した。これに伴い
      `src/app/api/**/route.ts`だけでなく、リポジトリ関数を直接呼ぶ
      Server Components(`characters/page.tsx`・`decks/page.tsx`・
      `battles/page.tsx`・`battles/[id]/page.tsx`・`characters/[id]/edit/page.tsx`・
      `decks/[id]/edit/page.tsx`)にも`await`を追加した(`battles/[id]/page.tsx`の
      `fetchBattle`/`fetchRoster`ヘルパーは関数自体を`async`化)。
    - `src/lib/db/client.ts`・`schema.sql`は削除(役目がPHP側`db.php`/
      `schema.sql`に移行)。`migrate.ts`/`seed.ts`は「remoteの`migrate.php`/
      `seed.php`を`PHP_BRIDGE_ADMIN_KEY`付きで叩くだけの薄いスクリプト」に
      書き換え、`npm run db:migrate`/`db:seed`のDXは維持した。
      `package.json`から`better-sqlite3`・`@types/better-sqlite3`・(使用箇所が
      無くなった)`uuid`を削除。
    - アップロード: `lib/uploads/storage.ts`はローカル`fs.writeFile`から
      PHPブリッジの`upload.php`へのmultipart転送に置き換えた(mime/サイズ
      検証はNext.js側にも残し、PHP側でも二重に検証)。旧
      `src/app/api/uploads/characters/[filename]/route.ts`(起動後アップロード
      ファイルが静的配信対象外になる問題への回避策)と`next.config.ts`の
      `rewrites()`は、画像が外部ホストから直接配信されるようになったため不要
      になり削除した。
    - 静的アセット: 新規`src/lib/assets.ts`(`ASSET_BASE_URL`=
      `NEXT_PUBLIC_ASSET_BASE_URL`、既定値はスターサーバーのURL、`assetUrl()`
      ヘルパー)を`src/app/page.tsx`(サンプル画像2枚)・`BattleSetupForm.tsx`
      (バトル背景テンプレート)・`musicController.ts`(`SOUND_DIR`)から利用する
      よう変更。`globals.css`の`.page-bg::before`は`layout.tsx`の`<html>`に
      設定したCSS変数`--hero-bg-url`を参照する形に変更(3ページ共通の
      `.page-bg`は`layout.tsx`配下で全ページに効くため1箇所の変更で足りる)。
      シードデータの`imageUrl`は相対パスではなく`ASSET_BASE_URL`を前置した
      絶対URLとして`seed.php`内でMySQLに投入するため、`character.imageUrl`を
      そのまま`<img src>`に使っている既存の描画箇所(`BattleSetupForm.tsx`等)は
      無修正で動作する。
    - `.env.example`・`.env.local`から`DB_PATH`を削除し、`PHP_BRIDGE_URL`・
      `PHP_BRIDGE_API_KEY`・`PHP_BRIDGE_ADMIN_KEY`・`NEXT_PUBLIC_ASSET_BASE_URL`
      を追加。`README.md`(技術スタック・必要環境・環境変数表・既知の制約)、
      `docs/設計.md`(冒頭に移行の経緯と「2章のDDLは`php/schema.sql`が正」
      である旨の追記)も更新した。
    - 動作確認: サンドボックスにPHP 8.4 CLI(標準)はMySQL拡張(`pdo_mysql`)を
      含まないビルドだったため、`sudo apt-get install php8.3-cli php8.3-mysql`
      で導入し`/usr/bin/php8.3`を使用。Dockerで`mysql:8.0`コンテナを起動し、
      `php8.3 -S localhost:8811 -t php`(ブリッジ)・`php8.3 -S localhost:8899
      -t public`(静的アセット代替)をローカル起動、`.env.local`を一時的に
      これらに向けて以下を確認(検証後いずれも停止・削除し、`.env.local`は
      スターサーバー向けの値に戻し、生成物`php/config.php`・アップロード
      検証用の一時画像は削除済み):
      - `npm run build`・`npx tsc --noEmit`・`npm run lint`が(移行前から
        存在する`BattleSetupForm.tsx`の2件のReact Compiler系lintエラーを
        除き)エラーなく成功。
      - `npm run db:migrate`(8テーブル作成、2回連続実行してもエラーなし=
        冪等性確認)→`npm run db:seed`(23キャラクター・2デッキ投入、
        再実行しても件数・IDが変わらないことを確認)。
      - `GET/POST/PUT/DELETE /api/characters`・`/api/decks`(一覧・詳細・
        作成・更新・削除、100ポイント超過400・使用中409・
        `CHARACTER_NOT_FOUND`時400・存在しないid404、をすべてcurlで確認)。
      - `POST /api/upload/image`(実PNG→200・絶対URL返却・ファイル実体が
        `public/uploads/characters/`に書き込まれ静的サーバーからも200で取得
        できることを確認、2MB超→400、非対応MIME→400)。
      - `POST /api/battles`(実際にOpenRouter経由でAI審判を呼び出し、
        `status:"completed"`・`battleLog`14件・`events`14件・`mvpCharacterId`が
        正しく解決されて201で返り、MySQL側`battle_logs`/`battle_events`にも
        同数保存されていることを確認)。`GET /api/battles`・`GET /api/battles/:id`
        も正常。
      - `curl`でSSR HTMLを取得し、トップページの`--hero-bg-url`・
        `/characters`一覧の`<img src>`・`/battles`ページがいずれも
        `ASSET_BASE_URL`(検証時は`http://localhost:8899`)起点の絶対URLに
        なっていることを確認(ヘッドレスブラウザは今回もサンドボックス
        未導入のため、実ブラウザでの目視確認は未実施)。
    - 残課題: 実際のスターサーバーへの`php/`フォルダ・静的アセットのFTP/SFTP
      アップロード、MySQLデータベース・ユーザーの作成、Render(またはNext.js
      稼働先)の環境変数(`PHP_BRIDGE_URL`/`PHP_BRIDGE_API_KEY`/
      `PHP_BRIDGE_ADMIN_KEY`/`NEXT_PUBLIC_ASSET_BASE_URL`)設定は、
      このサンドボックス環境から実行できる範囲外(認証情報が無い)のため
      未実施。デプロイ後に一度`migrate.php`→`seed.php`を呼び出して初期化する
      必要がある。また、指定URLは`http://`(平文)のため、スターサーバー側で
      無料SSLが使えるなら`https://`化を推奨する。

## 全体メモ・課題

(実装中に発生した横断的な問題・使用量制限による中断状況などをここに記録する)

## 最終確認(2026-07-03)

- 状態: **MVP実装完了**
- タスク一覧(1〜17)はすべて `[x]` 完了済みであることを確認。
- `npm run build`(Next.js 16 / Turbopack、TypeScript型チェック含む)を実行し、
  エラーなく成功することを確認(既知の `next.config.ts` 経由の
  「Encountered unexpected file in NFT list」warningのみ。これは
  `better-sqlite3` をNode.js runtimeで同期利用していることに起因する
  Turbopackのトレース警告であり、タスク5以降のメモに記載済みの既知事象で
  ビルド失敗ではない)。
  - ルート一覧: `/`, `/characters`, `/characters/new`, `/decks`, `/decks/new`,
    `/battles`, `/battles/[id]` の各ページと `/api/characters`,
    `/api/characters/[id]`, `/api/decks`, `/api/decks/[id]`, `/api/battles`,
    `/api/battles/[id]`, `/api/upload/image` の各APIルートがすべて正常に
    ビルドされることを確認。
- `npm run lint`(ESLint)を実行し、エラー・警告ともに0件であることを確認。
- `grep` で `src/` 配下に `TODO`/`FIXME`/`XXX` 等の残存マーカーが無いことを確認。
- 追加のランタイム動作確認として(既存の `data/game.db` を変更・削除せず)
  `npm run dev -- -p 4455` で開発サーバーを起動し、以下を `curl` で確認して
  正常終了・停止した:
  - ページ: `/`, `/characters`, `/decks`, `/battles`, `/characters/new`,
    `/decks/new` がすべてHTTP 200を返す。
  - API: `GET /api/characters`(シード済み16件)・`GET /api/decks`
    (サンプルデッキA/B 2件)・`GET /api/battles`(0件、配列)がいずれも
    期待通りのJSONを返す。
  - 軽微な修正が必要な問題は発見されなかったため、本タスクでのコード変更はなし。

### 残課題(既知・タスク完了メモに記載済みのもの含む、引き継ぎ事項)

- **有効な `ANTHROPIC_API_KEY` を用いたフルE2E未実施**: このサンドボックス環境には
  有効なAPIキーが無いため、「対戦セットアップ画面から実際にClaude APIを呼び出し
  対戦結果を生成・保存し、結果画面に反映される」という一連のAPI経由フルE2E確認は
  タスク8・11・15・16・17のいずれでも未実施(`saveBattleResult`等のリポジトリ関数を
  直接呼び出す代替検証・SSR HTMLのcurl確認までは実施済み)。本番相当のAPIキーを
  設定した環境での動作確認が必要。
- **ヘッドレスブラウザでの視覚確認が未実施**: Playwright/Puppeteer等がサンドボックス
  未導入のため、`BattleSetupForm` のスピナーアニメーション(タスク15)・
  `BattleEventCard` のフラッシュ/パルスアニメーション(タスク17)など、CSS
  アニメーション自体の実際の見た目はブラウザ実機での確認が望ましい(構文・静的
  レビューでの妥当性確認のみ実施済み)。
- **N+1クエリの許容**: `src/lib/characters/repository.ts` の一覧・詳細取得は
  MVPの想定データ量(数十件程度)を前提にシンプルさを優先しており、大量データ時の
  パフォーマンス最適化は未実施(タスク5メモに記載済み)。
- 上記以外に、各タスクのメモに記載された「残課題: なし」以外の個別懸念事項は無し。

以上により、設計書(`docs/設計.md`)に基づく全17タスクの実装・ビルド・静的検証・
簡易ランタイム検証が完了し、**MVPとして動作する状態である**ことを確認した。
